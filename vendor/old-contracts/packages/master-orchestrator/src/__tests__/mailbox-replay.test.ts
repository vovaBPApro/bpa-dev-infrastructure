import { test } from 'bun:test';

// PARITY-GUARD: D2; implementation module intentionally absent until the D2 restart/replay seam lands.
test.skip('D2 donor fixture: mailbox replay contracts', () => {});
