# HR-1382 — Infrastructure v3: orphan branch in this repo, marry both bases, Docker meteorite test, today on codex

date: 2026-08-02
source: Telegram, message 1382 (voice, whisper-local transcript)
status: binding
resolves: HR-1363 motion (he decided: build v3) — consilium round 2 now feeds the HOW
related: HR-1374 (two bases), HR-1379 (infrastructure scope), HR-1371 (context pack)

## Human words (verbatim, do not reword)

> Володня, давай інфраструктуру спочатку забезпечимо адекватно. Мені здається, тут найшвидше рішення, щоб я сьогодні ще встиг ввечері, ну, з тобою ми разом її встигли протестувати. Найшвидше рішення буде зробити наступну версію, третю получається, і там подружити дві попередні. По бранчах получається, ми просто зробимо бранч окремий, в якому збережемо всю поточну історію, і потім заведемо Окремий branch, в якому поточну історію всю видаляємо і в якому все ведемо з нуля. Новий репо я створювати не хочу. От, тіпа новий main получається робимо. Або просто v3 бранч і v3 бранч, от, можна ж його зробити, щоб він пустим був, і в нього просто почати новий проєкт, читай з нуля, з того, що копіювати з першої версії копіювати оці базові речі, які провалилися у другій, а з другої версії скопіювати нові речі, які наче виглядає, що вони працюють, але толком не протестовані через те, що базові речі не працюють. І получається, тобі треба організувати роботу так, щоб щас на кодексі це запіліть, реалізувать. Пару годин у нас є, дві, три, чотири години є, дуже багато квоти, є інфраструктура офігезна. Так от разом з ком- Сіліумом вирішать план, як оце все реалізувати. Тобто скупіювати ці дві частини, подружити їх, протестувати, переконатись, що вони працюють. Тестувати я пропоную в докері контейнер піднімати, щоб він самоналаштовувався заодно, тобто метеоритний тест робити. Ну і максимально паралеліть, швиденько. Раз, раз, раз, хоп, хоп, хоп, чик, чик, чик та поїхали вже, ребят. Ну саме цікаве попереду. Ну що відбувається, га?

## Ruling

1. **Build infrastructure v3 TODAY** (2-4 hours of parallel codex work), so he
   can test it together with the orchestrator this evening.
2. **Same repository, no new repo.** Branch scheme: one branch preserves the
   entire current history; a separate EMPTY branch (`v3`) starts the project
   from zero. Possibly it becomes the new main later.
3. **v3 content = marriage of two bases:** from version 1 (old orchestrator)
   copy the base mechanisms that failed in version 2; from version 2 (this
   tree) copy the new additions that look working but were never properly
   tested because the base underneath them was broken.
4. **The consilium decides the implementation plan together with the
   orchestrator** — the running infra lens lanes' port-list/keep-list outputs
   feed it directly.
5. **Testing = the meteorite test in Docker:** the container must
   self-configure from the repository alone.
6. Maximum parallelism on codex.

## Routing

- Branches: `v2-archive` (current history), orphan `v3` (empty root).
- Consilium round 2 verdicts → v3 execution-plan synthesis → parallel codex
  build waves on `--base v3` → Docker meteorite gate.
