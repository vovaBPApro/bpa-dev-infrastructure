# v3 workboard

The tracked plan for finishing v3. Built from measurements taken 2026-08-03, not
from estimates. Sources, all on `v2-deprecated`:
`instance/v3-mechanism-gap-2026-08-03.md` (126 missing mechanisms, 33 blocking),
`instance/consilium2-2026-08-03-v3-viability.md`, `instance/reap-executed-2026-08-03.md`,
`instance/report-pinning-convention-2026-08-03.md`.

Every row has an acceptance criterion that is a command, not a judgement. A row is
done when the command exits 0 at a landed SHA. Rows are ordered by dependency: a
later phase cannot start honestly until the earlier one holds, because the earlier
phase is what makes "done" measurable at all.

Operator constraints in force: HR-1720 (host deployment deferred to v3, so all of
this lands in the repo and is proven in a container, not by touching this host),
HR-1718 (v3 stays the working line; cutover last), HR-1726 (review rounds capped at
3, escalate to executable locks).

---

## Phase 0 — make the loop work at all

Nothing else can be trusted until a lane can finish, be reviewed, and land without a
human nursing it.

| id | row | acceptance | state |
|---|---|---|---|
| V3-0.1 | Port `gate/land.sh --target-branch` from the abandoned line. Without it landing is only possible onto the repository default branch. | `env -u BUN_BIN bash gate/land-target-branch.test.sh; echo $?` → 0 | **done** — landed `f331295`, `verdict=landed review=accepted`. See "Nested gate invocations" below: the apparent flakiness was the verify command, not the change. |
| V3-0.2 | Lane terminal reports: enforce the corrected convention. The report is an external file naming the branch tip; a report committed into its own branch can never match the tip (proven, see `report-pinning-convention`). Wire `gate/completion-guard.ts` at lane exit, not only at land time. | a dispatched lane cannot end while `bun gate/completion-guard.ts --report <path> --repo . --branch <tip>` exits non-zero | **partial** — `gate/lane-exit.sh` wraps `gate/completion-guard.ts` and is taught in `instructions/lane-lifecycle.md` (coder baseline, self-check) and `instructions/orchestrator-cold-start.md` §2.5 (orchestrator, right after the lane reports done). See "Two live report contracts" below: `orchestrator/dispatcher.ts`'s separate inline check was left un-reconciled, on purpose, and is documented as remaining work. | **done** — the reconciliation this row deferred landed as V3-0.16 at `39a7698`: `orchestrator/dispatcher.ts` now imports `completionReportVerdict` from `gate/report-contract.ts`, which invokes the authoritative `gate/completion-guard.ts`. Verified on `origin/main` 2026-08-04. |
| V3-0.3 | Landing gate rollback defect: an aborted landing left local `main` advanced to the merged commit with a dirty tree. Origin was never touched, so the push boundary held, but the local ref moved after `verdict=aborted`. | a test that aborts a landing at post-merge-verify and asserts `git rev-parse <target>` is unchanged | **done** — landed `5f24575`. The gate no longer prints `verdict=aborted` while leaving the target advanced; rollback is verified on ref position AND tree cleanliness, and reports `verdict=rollback-failed` when restoration cannot be proven. Took three review rounds and closed by the HR-1726 lock exit; lock verified red at `5d1c16d`, green at `45a395a` by the orchestrator directly. |
| V3-0.4 | Ledger drift check — **done**, landed 2687420 + 76c5363. | `bun test tools/check-decision-ledger-drift.test.ts` → 2 pass | **done** |
| V3-0.5 | Make the lane-exit guard structural rather than advisory. Independent review of V3-0.2 confirmed nothing in `orchestrator/`, `core/` or the watchdog invokes `gate/lane-exit.sh` — it is delivered as instruction prose only, so a lane can skip it. Correct, fail-closed logic with no executor is this repository's dominant defect shape. | `grep -rn "lane-exit.sh" orchestrator/ core/` returns a real caller, and a lane that skips it cannot report done | **partial** — investigation found no single choke point: real AI coder-lane dispatch is entirely prose-mediated (`instructions/orchestrator-cold-start.md`), `orchestrator/dispatch-lane.sh` gates only launch (the prompt marker), and `orchestrator/dispatcher.ts`'s fenced-dispatch `validTerminal()` is exercised only by `tests/fixtures/*.ts` synthetic workers, never by a real lane. `core/schema.ts`'s `DurableStore.completeLane()` — the one method both callers funnel through — is deliberately storage-only (`core/schema.test.ts`, `core/state.test.ts` exercise it with symbolic, non-filesystem evidence on purpose) and correctly cannot host a git-aware gate. `core/mission-cli.ts`'s `lane complete` action is the one real, git/filesystem-aware caller: it took a bare `reportPath` string and never read it. It now shells out to `gate/lane-exit.sh` (env stripped of `BUN_BIN`, see "Nested gate invocations") before calling `completeLane`, requires a `--branch`-equivalent 7th argument, derives the recorded sha from the guard-checked report rather than trusting the caller, and cross-checks the caller's claimed verdict against the guard's. `grep -rn "lane-exit.sh" orchestrator/ core/"` now returns `core/mission-cli.ts`. Proven with a real caller-level before/after: pre-change, `lane complete` recorded `terminalVerdict: clean` for a fabricated sha and a reportPath that did not exist on disk (captured output in the terminal report); post-change the identical call is refused (`ERROR GATE ... FAIL report-file missing`, exit 1, lane state stays `running`). `core/mission-cli.test.ts` adds the three required rejection shapes (intermediate SHA, missing report, report committed into its own branch) plus a same-owner/token retry-after-rejection case and a claimed-verdict-vs-report mismatch case, all proven red before the fix and green after. `orchestrator/dispatcher.ts`'s parsing hardened too (new `gate/report-contract.ts` shared, anchored `lineValue()` parser replaces a `report.includes()` substring check that a "line-injection" regression test proves was foolable), but its `validTerminal()` still runs its OWN bespoke fencing contract, not `gate/completion-guard.ts`'s — reconciling that still needs the branch-field schema change V3-0.2 flagged as bigger than one row, since it is exercised only by synthetic fixtures with zero real-lane traffic today. **Remains, next row:** (a) real AI coder-lane dispatch still has no code-mediated choke point at all — nothing in this repo can stop an LLM orchestrator from simply not calling `mission-cli.ts lane complete`, only make that call gated when it IS made; (b) `orchestrator/dispatcher.ts`'s fenced protocol is still a second, unreconciled report contract per "Two live report contracts" below; (c) `instructions/orchestrator-cold-start.md` references `mission transition`/`lane transition` CLI actions that do not exist anywhere in `core/mission-cli.ts` — found during this row's investigation, flagged in the doc itself, not fixed here (mission/manager have no state-machine transitions at all beyond their fixed creation state). | **done** — landed `f620e76` `[CODER] enforce terminal reports at lane process exit` and `491af5d` `[CODER] make nested lane exit gate-safe`. Verified on `origin/main` 2026-08-04: `orchestrator/fleet/launch-lane.sh:165` invokes `gate/lane-exit.sh` on every lane, so the guard is structural rather than advisory. The `mission-cli` documentation gap it flagged is its own row, V3-0.15. |
| V3-0.6 | **New.** The fleet counter measures the wrong thing. The autonomy nudge reports `running lanes=0` while lanes are demonstrably working, because `tools/state-contract/check.ts:FLEET-IDLE` counts system lane units (tmux sessions) and not lanes running as separate agent sessions. A counter that cannot see the work it exists to measure will report FLEET-IDLE during a busy night and silence during an idle one. | seed N running lanes by the mechanism actually used for dispatch; the reported count equals N | **done (PARTIAL)** — landed `1f30726`. `core/mission-cli.ts lane complete` took a report path and NEVER READ IT; a lane could record itself terminal pointing at a nonexistent file. It now shells out to `gate/lane-exit.sh` and derives the recorded SHA from the guard-checked report. Also replaced `dispatcher.ts`'s foolable `report.includes()` substring check with a shared anchored parser (`gate/report-contract.ts`) — a forged report used to pass. Open: no code choke point forces an orchestrator to call the gated CLI (no launcher on v3); `dispatcher.ts`'s fenced protocol is a second contract, confirmed inert; `orchestrator-cold-start.md` cites `mission/lane transition` commands that do not exist. | **done** — the fleet counter reports truthfully: the autonomy nudge reported `running lanes=1` and `running lanes=5` matching the actual lane processes on 2026-08-04. The two other defects named in this row were split out and are tracked separately: the second report contract closed as V3-0.16 (`39a7698`), and the nonexistent `mission/lane transition` commands are V3-0.15 (in review). |
| V3-0.7 | **New.** `refs/stash` is repo-global, not per-worktree. Two lanes running in sibling worktrees of this repo share one stash stack; that collided today and pulled a foreign file into the wrong worktree. At the fleet floor of 10 this silently mixes two agents' work, and no test would show it because each file stays individually valid. | two concurrent lanes each stash and restore; neither sees the other's entry | **done** — implemented, scratch commits are worktree-local; `hygiene/check-shared-stash.sh` rejects a non-empty shared stash |
| V3-0.8 | **New.** `bootstrap/check-unit-drift.sh`'s manifest is not anchored to anything harder to edit than itself: deleting a template AND its `expected-units.tsv` line passes silently (reproduced in review). | a test asserting the manifest lists at least the units the 2026-08-01 incident named — orchestrator, watchdog service and timer, telegram-daemon | **done** — landed `1f54a12`, review ACCEPT with the coupled-delete reproduction executed. The four incident units are pinned by name in a second, independent place, so deleting a template AND its manifest row now fails. |
| V3-0.9 | **New.** The ledger drift check resolved only a local `v2-deprecated` branch, so a fresh clone with only `origin/v2-deprecated` failed closed despite carrying the donor ref. | `bun test tools/check-decision-ledger-drift.test.ts` includes a temporary repository where the donor exists only at `refs/remotes/origin/<name>` and exits 0; an absent donor still fails closed | **done** — the checker tries the caller's ref first, then its `origin/` remote-tracking name, without fetching or writing refs; the regression lock was red before the change and all 3 cases are green after it. |
| V3-0.10 | **New.** The privilege-drop fixture in `hygiene/reap.test.ts` inherited an untraversable lane `TMPDIR`, so it failed before reaching the unreadable protected-list behavior. | both ambient and default `TMPDIR` runs pass; an explicit dropped-privilege traversal/execute probe names fixture setup failures; fail-open mutation makes the regression case red | **done** — the copied toolroot now uses the system temporary directory derived with ambient temp variables removed, is always cleaned up, and its unprivileged traversal/execute precondition is checked before the subject assertion. |
| V3-0.11 | **New.** The landing gate collected no `*.test.sh` files, leaving all nine tracked shell tests inert, including the gate's own regression suite. | the gate-collected `tools/shell-test-tier.test.ts` independently pins all nine paths, executes every runnable test with `BUN_BIN` cleared, and names every measured exclusion with its blocker | **done** — landed `604de9f`. `tools/shell-test-tier.test.ts` executes the tier at every landing, with an explicitly pinned inventory (a glob cannot detect a deleted test). Measured cost ~48s per sweep. Two tests were excluded with named reasons; both closed later the same day, see V3-0.12 and V3-2.5. |
| V3-0.12 | **New.** Close the two measured shell-tier exclusions without weakening their subject assertions. | both excluded shell tests pass; the pinned inventory remains nine; `excludedShellTests` is empty; subject mutations prove both locks red | **done** — `gate/land-rollback.test.sh` landed `66077cd` (root-safe privilege-drop fixture with a checked precondition; review ACCEPT after reproducing that the lock still catches a broken rollback). `orchestrator/watchdog.test.sh` could not be fixed as a fixture — its failure was the production contract defect filed as V3-2.5 — and closed with it at `7149da8`. `excludedShellTests` is now empty: all ten tracked shell tests run at every landing. |
| V3-0.13 | **New.** A terminal report could assert a completed independent review in a `review:` field without any review artifact; the completion guard ignored the claim even though the landing gate correctly rejected it. | the guard rejects `review:` without a sibling `<branch>.review.md`, accepts an `ACCEPT` artifact naming the branch tip, rejects a different `reviewed-sha`, and still accepts `remaining: Tier-A review pending` without `review:` | **done** — landed `a974e05`. `gate/completion-guard.ts` refuses a report asserting a `review:` verdict without a matching ACCEPT artifact for the branch tip, derived by the same convention as `gate/land-lib.sh:319`. Three implementation rounds under the HR-1726 cap: round 1 was evaded by `Review:`/` review:`/`review :`; round 2 suppressed fenced examples and the skip became a hiding place (one unmatched fence hid every later claim to EOF); round 3 rejects unterminated fences. Verified at the landed SHA: a bare fabricated claim gives `FAIL review-artifact missing`, an unterminated fence gives `FAIL review-field unterminated-fenced-block`, and `remaining: Tier-A review pending` with no field still passes. Two rebases were needed because `2f023ad` landed into `instance/workboard.md` while the row was in review — orchestrator error, recorded in the sprint close. |
| V3-0.14 | **New.** Required GitHub refs existed only as mutable remote state, with no tracked authority or fail-closed measurement of deletion protection. | an independent ref spec plus stubbed verifier locks for missing credential, API error, absent, unprotected, malformed, and fully protected responses | **implemented** — tracked spec and landing-executed verifier added; live GitHub configuration still requires a credential and independent application. |

## Phase 1 — a clean server can be rebuilt from this repo

The 33 blocking mechanisms from the gap inventory. Until this phase holds, wiping the
host is unrecoverable and Hard Floor 5 is not satisfied by v3 alone — the only copy of
the rebuild path is on `v2-deprecated`.

| id | row | acceptance | state |
|---|---|---|---|
| V3-1.1 | Port/rewrite `bootstrap/` — `install.sh`, `deploy-host-mechanism.sh`, `check-unit-drift.sh`, `check-deployed-drift.sh`, `telegram-transport-preflight.sh` and their tests. | `bash bootstrap/bootstrap.test.sh; echo $?` → 0 | **stage 1 done** — landed `88361ff`. Ports `ensure_prerequisites`, `install_bun`, `sync_repository`, `render_environment`, `initialize_state_db` with `--dry-run`/`--verify-source`, validated by stub fixtures the way the donor does, not a container. Review round 1 rejected it twice over: `sync_repository` fast-forwarded whatever branch happened to be checked out (on this host that is `v2-deprecated`, so it would have reported a clean bootstrap while leaving the machine on the abandoned line), and `unzip` had been trimmed although `bun.sh/install` hard-fails without it. Both fixed and locked. Still later rows: `--verify`, `activate_units`, watchdog arm/disarm. **Stage 2 PARKED 2026-08-03** — `install_hygiene_cron`, `run_install_test_gate` and `render_units` exhausted the HR-1726 three-round cap without an ACCEPT. Round 3's blocking finding, reproduced by the reviewer: `unit_publication_signal` resets HUP/INT/TERM to default before rollback begins, so a second signal during rollback leaves a half-restored unit set and emits neither `rolled-back` nor `rollback-failed`. Work retained on branch `ag-s3-2-r3` (`09082ecdf1d41c88f49a8a83b957aa46958e6e94`, pushed to origin, do not reap). Evidence, what the three rounds did establish, and the lock the recut must satisfy: `instance/parked/V3-1.1-stage2-2026-08-03.md`. |
| V3-1.2 | Port the 17 unit templates under `bootstrap/units/`, including `bpa-telegram-daemon.service.in` and `bpa-orchestrator.service.in` — the units that start the system at all. | **done** — landed `51ae687`. Templates plus a drift checker with an INDEPENDENT manifest (`instance/expected-units.tsv`), so a deleted template is caught and not only a changed one; round 1 was rejected because deleting the watchdog template produced exit 0 in silence. Product-named `agentic-bpa-*` units moved to `instance/units/` per HR-309. Follow-up filed: a test pinning the units the 2026-08-01 incident named, so a coupled delete of template AND manifest line cannot pass. |
| V3-1.3 | `tools/whisper/install.sh`. v3 has `daemon/transcribe.ts`, which calls whisper, and nothing that installs it. Raised by the operator directly (Telegram 1760). | in a clean container: install, then `$WHISPER_BIN --version` exits 0, then a voice fixture transcribes | **landed `fac4f35`, container proof deferred.** **Container proof now closed — landed `e32efa5`.** Installer and 9 tests landed; an independent supply-chain review returned ACCEPT and answered yes to running it as root on a clean server. Source pinned by 40-hex commit (a moved tag aborts the build), model checksum verified before the file is moved into place, installer paths byte-identical to `resolveWhisperConfig()`. NOT proven without a container: a real model download against production URLs, a non-stubbed transcription, the `apt-get` branch. Blocked on V3-1.1. |
| V3-1.4 | `hygiene/reap.sh` + `install-cron.sh`, and an executor. On the old line the cron was tracked and never invoked (`crontab -l` → "no crontab for root"), which is why 1372 branches accumulated. | reap runs on a timer in the container and a seeded stale branch disappears | **done** — landed `d7abf55`, 18 tests. Three review rounds, each catching a different route to deleting the branch `v3`: a missing protect list, a trailing blank line killing the script under `set -e`, and a final line without a trailing newline. Closed by the HR-1726 lock exit; lock verified red at `fec502d` (16 pass, 2 fail), green at `a170a53`. Cron arming still deferred to V3-1.1. |
| V3-1.5 | `meteorite/run.sh` — the clean-rebuild proof itself, absent from v3 while the plan measured v3 against it. **Re-scoped 2026-08-03:** the donor is a *product* meteorite (clones `PRODUCT_GIT_URL`) and no product repo exists; the infrastructure meteorite was never a script, only a hand-run procedure that FAILED on 2026-07-31 (`v2-deprecated:reports/meteorite-test.md`). | `bash meteorite/run.sh` from a clean clone in Docker exits 0 | **done** — recut landed `133541c` after the park. The leak the park record named is closed structurally: candidate refs are published into a sweepable `refs/meteorite-candidates/*` namespace that `hygiene/reap.sh` clears, so a signal or crash leaves no ref that nothing owns, and `meteorite/prove-candidate.sh` is the supported entry point. **Run end to end by the orchestrator at `e17f629`: `result: clean`.** Clean `ubuntu:24.04`, credential-free clone from GitHub, no file from this host, every stage executed, both temporary refs auto-deleted (`git ls-remote origin 'refs/meteorite-candidates/*'` → 0 lines). The same proof returned 17 failures the same morning; the causes were closed by `787bb40` (ledger drift in a fresh clone) and `7a28088` (the two container-capability exclusions, V3-1.8). Hard Floor 5 is measured rather than asserted for the first time. Still unproven by the meteorite and named in its own report: unit activation, watchdog arm, Telegram transport, and the three named capability exclusions. Follow-up filed as V3-2.6 (its blocker message does not name its missing inputs). Park record retained at `instance/parked/V3-1.5-meteorite-2026-08-03.md`; branch `ag-s3-1-r3` no longer needs protection. |
| V3-1.6 | **New 2026-08-03.** v3 has no way to launch a coder lane at all. `orchestrator/` on this line carries `launch.sh`, `dispatch-lane.sh` and `dispatcher.ts`, but the mechanism that actually starts an agent session — `orchestrator/fleet/launch-lane.sh` — exists only on `v2-deprecated`, and every lane dispatched today was launched from that v2 working tree. Consequences: (a) a host rebuilt from v3 alone cannot dispatch any lane, so Hard Floor 5 is not satisfied by v3 even once V3-1.5 lands; (b) the mission→manager→lane hierarchy is enforced in data (`core/schema.ts` `createManager` requires depth 1 and parent=mission; `createLane` requires parent=manager) and the manager role pack composes, but no manager can raise a lane, so the three-level hierarchy is structure without an executor — the repository's dominant defect shape. Raised by the operator directly (Telegram 1874). | from a clean container holding only this repository, dispatch a lane and have it report terminal | **done** — landed `d4dee39`. Ported generic, with the agent command as data in `instance/lane-agent-command.conf` (HR-309), per-lane 0700 TMPDIR, and an atomic reservation after review round 1 found the `[[ ! -e ]]` check raceable. Proven by the orchestrator launching a real lane from the landed SHA: systemd unit, fresh worktree, live agent replying `GPT-5 Codex / ag-v3landed`. v3 can now dispatch on its own; every lane before this was launched from the v2 tree. |
| V3-1.7 | **New 2026-08-03, operator requirement HR-1876.** The agent hierarchy the operator wants is dispatcher → second-level agents → third-level "junior" agents that second-level agents raise themselves for a sub-task. v3 does not just lack this, it forbids it: `core/schema.ts` fixes the tree at mission(0) → manager(1) → lane(2), `createLane` requires a lane's parent to be its manager, so a lane cannot parent a lane and can only ever be a leaf. `instructions/roles.md` also treats the manager as optional and non-bounding, matching the operator's sense that the manager layer was set aside. Blocked on V3-1.6 (nothing launches an agent on this line at all) and on the operator restating the model — his words are the requirement, not a paraphrase of them. | a second-level agent raises a third-level agent, the third reports terminal to the second, and the second's rollup is what the dispatcher sees | not started — awaiting operator restatement, see `instance/decisions/HR-1876.md` |
| V3-1.9b | **New 2026-08-03. The lane network boundary is silently ineffective for the production identity.** Executed under the freshly provisioned `bpa` user manager: the plain localhost probe exited 0, and the **identical** unit with `IPAddressDeny=localhost` / `IPAddressAllow=127.0.0.53` **also exited 0**. Under the *root* user manager the same pair gave `exit=0` / `exit=28`, so the property was assumed to hold generally — it does not. `systemd.resource-control(5)` warns `IPAddress*` silently has no effect where cgroup/eBPF support is unavailable, and that is what is happening for the unprivileged user manager. This is the exact false-green shape Hard Floor 7 forbids: the property is serialized into the unit, the unit starts, nothing errors, and the confinement does not exist. Every earlier statement that lane network confinement is enforced was measured on the wrong identity and must not be relied on. | under the `bpa` user manager the filtered probe fails to reach localhost, or the launcher **refuses to start a lane** with a named capability blocker; a test locks both directions |
| V3-2.8 | **New 2026-08-03. A landing-gate test depends on mutable host state.** `orchestrator/model-pin.test.ts:87-93` ("matching pin proceeds into the startup path") runs the real `orchestrator/launch.sh start` and asserts the exit code produced by a live auth preflight (`expect(result.exitCode).toBe(2)`). It passed when its lane verified and reported `result: clean`; roughly an hour later, with **no change to the branch**, it returns 1 and blocks landing. Confirmed not to be a merge interaction: it fails on the unmerged lane branch too, so the only variable is the host, which changed when the `bpa` account was provisioned under HR-1962. A test whose verdict depends on host state outside the repository cannot survive a clean rebuild either, so this is a Hard Floor 5 exposure as well as a flake. | the test asserts the pin boundary against a stubbed preflight, is green on a host with and without provider auth, and is green inside the meteorite container | **in progress on `ag-s6-7b`.** Two things established by the first, correctly-refused attempt: (a) the subject does **not** exist on `main` — `orchestrator/model-pin.test.ts` and `model-pin.ts` live only on `ag-s5-5-r3`, V3-2.4's parked branch — so the row must be based there, and the fix therefore carries the parked work with it under one independent review; the orchestrator scoped the first dispatch against `main` without checking where the subject lived. (b) The host-dependence is a **single instance, not a pattern**: `orchestrator/launch-handshake-bounded.test.sh` and `orchestrator/singleton-failclosed.test.sh` also spawn `launch.sh start` but already inject local preflight stubs, and no other live-preflight test exists on that branch. |
| V3-1.9a | **New 2026-08-03, operator decision HR-1962 `provision_now`.** The `bpa` service account is now provisioned on this host — uid 1000, home `/home/bpa-shell`, `Linger=yes`, owned repository and worktree roots — so every conclusion V3-1.9 could only reach by reading is now executable. Two defects surfaced while provisioning: `bootstrap/provision-service-user.sh:16` invokes `useradd` bare and died with `useradd: command not found` because `/usr/sbin` is not on the orchestrator's PATH (it lives at both `/sbin/useradd` and `/usr/sbin/useradd`); and the script has no counterpart that de-provisions, so the only rollback is hand-typed `loginctl disable-linger && userdel -r`. A bootstrap step that cannot be undone by the repository is a Hard Floor 5 gap. Provider credentials remain operator-only (interactive `codex login` / `claude` as that user) and are deliberately not automated. | the provisioner runs with a minimal PATH, a tracked de-provision path restores the host, and both are proven in a container **in flight** as `ag-s10-7`. | **done** — landed `48a59b3` (`ag-s10-7-r3`, work `38159ff`), round 3 ACCEPTed by an independent reviewer who read the prior artifact only after finishing its own execution. Twelve findings, nine closed by execution, three left open and low by the reviewer's own judgement. The round-2 blocking defect is closed: a `state=creating` marker had deleted an account the provisioner never created (`T4_ACCOUNT=DELETED T4_DATA=DESTROYED`), and at round 3 the same path leaves `account=present data=intact`. **The landing took two attempts**: the first round-3 ACCEPT was refused as `malformed-artifact` because the reviewer wrote `identity:` where `gate/land-lib.sh:473-484` requires `reviewer:` (V3-0.39) — the artifact was preserved, not edited, as `ag-s10-7-r3.review.prior-identity-field.md`; the second attempt aborted at `push` because the orchestrator pushed bookkeeping to `origin/main` while the gate was running, and the rollback returned `land-main` to its pre-merge state with a clean tree. |
| V3-0.23 | **New 2026-08-03. The shell tier is not concurrency-safe, so suite results depend on what else is running.** A lane reported its declared suite passing, then failing on three consecutive completion-guard reruns — in tests unrelated to its change: `orchestrator/fleet/launch-lane.test.sh` and `orchestrator/singleton-failclosed.test.sh`. Independently, a reviewer's full-suite run failed on the pre-existing shared ref `ag-fleet-launch-proof` left by another lane's fixture (row V3-0.20), and two more lanes reported the suite "terminating without a decidable triple" which turned out to be their own timeout under load. Common cause: shell-tier fixtures contend for **host-global** resources — repository refs, singleton lock files, fixed paths — so at the fleet floor of 10 the same commit can be green or red depending on scheduling. Consequences are worse than flakiness: a lane cannot trust its own green, `gate/land.sh`'s framework check inherits the same contention at landing time, and every "unreproducible" report this evening becomes ambiguous between a real defect and a collision. | the shell tier passes with N lanes running concurrently, proven by executing it under deliberate contention; fixtures allocate private refs, lock paths and ports, and a test fails if a fixture touches a host-global name **recut 2026-08-04 and in flight as `ag-s10-8`** — no longer a background annoyance: under fleet load `bun test` returns **exit 0 after ~63 lines** with no trailing triple and without running the pinned shell tests, which is a false green at the harness level. It has now blocked V3-0.29, caused the REJECT of V3-0.20, and produced the original `ag-fleet-launch-proof` collisions. Measured contrast: fleet quiet at `5a2bc9e`, twice, `552 pass / 3 skip / 0 fail` in 133s and 126s. The counter reports `admissible round=0`, so the earlier HR-1726 park was policy, not mechanism. | **RECUT REQUIRED 2026-08-04 — the premise moved to V3-0.51 and round 3's price is refused.** Round 3 (`26b253a`, `ag-s10-8-r4`) closed the round-2 blocking finding: the stall watchdog no longer decides later than the runner containing it, which was a real arithmetic defect proven by its reviewer. **But its landing aborted at `meteorite rebuild-proof-timeout`** — the required end-to-end lock costs 395 s, and the meteorite's 900 s budget must also cover an apt install, a clean clone and two in-container suite runs. The rollback was clean and `origin/main` was never written. The lane also reports the suite at **557 s** with its change in, against 98-155 s before: a 4.6x cost. **The orchestrator refuses that trade, and states why rather than parking it silently.** This row's recorded symptom — exit 0, truncated output, no trailing triple — is now explained by V3-0.51: the lane harness kills a foreground command at exactly 2m00s with exit 143, and 143 through an unguarded pipe becomes 0 (V3-0.40). Two independent parties measured the axe and the orchestrator reproduced it directly. So the expensive serialisation this row was building is not what fixes the symptom, and paying 4.6x on every future landing to keep it is the wrong purchase. The lane said so itself: *"the premise answer was superseded mid-lane by V3-0.51 and is corrected above — the symptom is real, its cause is the harness, and this change does not address it."* That is the honest reporting this row's whole history was missing. **What survives**: the watchdog-race fix is a genuine defect closed on its own merits, and the round-2 reviewer's arithmetic proof stands. Recut it as a small change that keeps the watchdog correction and drops the 395 s end-to-end lock, after V3-0.51 lands and removes the axe. Nothing downstream waits on this row — V3-0.29 round 4 produced a clean report while it was open, which settles the gating question empirically. |
| V3-0.22 | **New 2026-08-03. Fixes relocate defects instead of closing them, and it reads as progress.** Three independent rows in the same review round were rejected with the reviewers' own word: **MOVED**, not closed. V3-2.3 gave the drift probe an "independent" caller — an unarmed unit, so still nothing runs. V3-3.4 made a fresh clone able to land by auto-initializing empty round state — so a new clone now bypasses an already-exhausted cap. V3-3.5 extracted the decision helper and tested it directly — so the untested part became the adapter wiring the helper to the real handler. Each fix is locally reasonable and each leaves the original failure reachable. The common tell is that the **new evidence would not catch the original failure**, which is a question a lane can ask itself before finishing and currently nothing prompts it to. Reviewers caught all three only because they were asked to classify findings as closed / moved / open rather than answer yes-or-no. | `instructions/` names the moved-not-closed failure and requires a lane to state, for each closed finding, what its new evidence would catch; the review artifact requires a per-finding closed/moved/open disposition | **done** — landed `636f0c7` `[CODER] classify moved findings explicitly`. Verified on `origin/main` 2026-08-04: `instructions/review-policy.md:44` requires every finding to be classified `closed`, `moved`, or `open`. The rule has since caught findings in three separate reviews. |
| V3-0.24 | **New 2026-08-03. CORRECTED — the disagreement was the orchestrator's method, not the mechanism, and not the lanes.** Filed on the belief that the meteorite gave different verdicts for one SHA depending on the runner: two lanes reported `clean`, one with two independent runs, while the orchestrator got `NO-GO` at the same SHAs. **The cause was that the orchestrator ran `meteorite/prove-candidate.sh` from a clone checked out on `main`, testing candidate SHAs whose fix was *in the runner script itself*** — `meteorite/run.sh` differed by 12 lines between the two. The old runner could not exhibit the new behaviour, so the verification was structurally incapable of confirming the fix. Proved by running the same SHA from the lane's own worktree (`result: clean`) and from the stale checkout (`NO-GO`, 19 failures) minutes apart. Earlier hypotheses were tested and disproved along the way: a divergent donor ref (identical at `08b98551` everywhere), and shared host networking (the lane demonstrated bridge networking with no host mounts or published ports, clean under deliberate CPU load). **The lanes were honest.** One of them was pushed to retract a truthful report because of this framing, and that retraction should not stand against it. Retained as a lesson rather than closed silently: a verifier running a different version of the mechanism than the artifact under test is measuring nothing, and this cost four container runs and two unwarranted doubts. | the proof records the runner's own version alongside the tested SHA, and refuses or warns when the runner is not the version being tested |
| V3-0.21 | **New 2026-08-03. Hard Floor 5 regressed the same day it was first proven, and nothing noticed.** The meteorite returned `result: clean` at `e17f629` this evening. Re-run by the orchestrator at `bf7c9b0` after the day's landings it returns **`NO-GO`, blocker `bootstrap-install command failed`**, with 20 test failures inside the container. Stages `container-start`, `prerequisites`, `clone`, `sha-verification` and `bootstrap-dry-run` all PASS; `bootstrap-install` is where it dies, and a lane that hit the same wall independently diagnosed it as gate fixtures reporting **`bun-not-found`** — a tracked bootstrap Bun-discovery defect. **The structural half is the important half:** `gate/land.sh` runs the suite on *this* host, where Bun is already installed, so a bootstrap regression that makes a clean rebuild impossible passes every landing check. The repository's central claim — destroy the host and the repository restores it — can therefore go false between proofs with no signal at all, which is the same "no executor" shape as the unarmed watchdog and the uninvoked reap cron, applied to the Hard Floor itself. | the bootstrap Bun discovery works in a clean `ubuntu:24.04` container and the meteorite returns `result: clean` at a landed SHA; **and a landing that touches a container-affecting path cannot complete without a rebuild proof** | **fix landed `2e62608`, structural half still open — and demonstrated twice.** The Bun-discovery and ambient-state causes are fixed: `ag-s7-1` was clean three times on its own branch, once under deliberate load, with an independent ACCEPT. But the meteorite at the **landed** SHA is `NO-GO` again, one failure: `shell tier: bootstrap/provision-service-user.test.sh`. That test arrived with `a08f214`, which the orchestrator landed an hour earlier — it passes on this host and fails in a clean container, so `gate/land.sh` (which runs the suite here, where Bun and the sbin PATH already exist) reported a clean landing. **So Hard Floor 5 regressed twice in one evening, both times through a green landing.** The mechanism is not the problem; the gate having no container-side check is. Until a landing that touches `bootstrap/`, `meteorite/` or the unit templates must produce a rebuild proof, the repository's central claim can go false at any commit and nobody learns until someone runs the proof by hand. | **done — landed `ff152de`.** The immediate breakage is fixed and, more importantly, the structural hole is closed: a landing that touches a rebuild-affecting path must now produce a meteorite proof, and the verdict comes from a **trusted pre-merge observer** snapshotted into a detached worktree, not from a script the candidate supplies. Three rounds to get there — round 1 was defeated by a prover containing `exit 0`; round 2 by `exit 0` plus a well-formed report with all eleven `PASS` stages and no container at all; round 3 moved the observer out of candidate reach. The lane also found on its own that the trigger was computed **after** the merge, collapsing `main...candidate` to empty and skipping the proof entirely. Declared residue, stated rather than implied away: a change to the prover itself is not trusted by its own landing — it becomes the trusted observer only after independent acceptance, and is exercised by the next applicable landing. Verified by the orchestrator at the landed SHA from a checkout at that SHA: `[meteorite] clean: ff152ded5079d98e231ad6f03b93a344fc699f0b`, exit 0, 0 fail. Measured cost ≈3m40s while the serial landing lock is held. **First save recorded 2026-08-04:** the gate refused a landing that had already earned an independent ACCEPT — V3-2.1's `bootstrap/systemd-recovery.test.sh` passes on this host and fails in a clean container, so the row that makes the orchestrator survive a rebuild would have broken the proof that rebuilds work. `LAND meteorite blocker=rebuild-proof-failed`, `verdict=aborted sha=none`, rollback clean, `origin/main` unmoved. The two regressions on 2026-08-03 were the same class and both shipped; each was found hours later only because the orchestrator ran the proof by hand. | **done** — landed `f188f7e` `[CODER] keep landing fixtures behind meteorite boundary`; the counter records it landed at that SHA. |
| V3-0.29 | **New 2026-08-04. A rule the Human sets must be one the Human can release.** The no-progress detector parked V3-1.9 — a row with three independent ACCEPTs, blocked only by V3-0.25 — and the Human answered `decision:v3_1_9_noprogress_park_2026_08_04=unpark_land`. **His instruction cannot be executed.** `gate/review-rounds.ts:64,68` refuses `check` and `attempt` whenever `item.park` is set and there is no reset or override; `instructions/review-policy.md:77` states this deliberately and a reviewer confirmed `override` exits 2 with `unknown-command`. Attempted: `REVIEW_ROUNDS status=fail detail=item=V3-1.9 parked=no-progress`, `LAND verdict=aborted`. So the mechanism has an **agent** boundary and **no operator path** — the right half built, the other half missing. The orchestrator deliberately did not delete the durable attempt refs by hand: that is indistinguishable from an agent bypassing its own counter, and deleting a ref with its mirror is exactly what the mirror design detects. | an operator-authorised unpark exists that no lane or orchestrator can forge, is durably and legibly recorded against its decision id, leaves cap and no-progress behaviour otherwise unchanged, and does not erase the history the park preserves | **round 3 REJECTed 2026-08-04, round 4 running as `ag-s10-1-r4`.** The review (`ag-s10-1-r3.review.md`) is the most valuable artifact this row has produced and every finding was executed, not argued. **F1, blocking: a lane authorises itself with one local `git update-ref`.** `gate/review-rounds.ts:190` resolves the trust root as `refs/remotes/origin/<target>` — a *local* ref in a Git common directory shared by ~90 lane worktrees — so the reviewer landed a park release end to end with a decision file origin never held, and `main` now carries `source: instance/decisions/HR-5001.md` as provenance for a file that never existed there. It falsifies this branch's own binding text ("not the working tree, not a stash, not a local branch") and does not become safe after V3-1.9. **F2, blocking: an aborted landing strands the decision forever** — both unpark authorities run *after* the attempt-ref replay loop, whose first line dies on `item.park`, so the authorised landing pushes its attempt ref, aborts, and every later landing dies before either authority is consulted. Two landings aborted on this installation today; HR-2149 in flight would have been spent for nothing. **F3, blocking**: the reserved-path loop omits `-c core.quotepath=false` that both neighbouring guards set, so a decision file whose name Git quotes is never checked — a self-authored `HR-9999é.md` landed. **F4, blocking**: one bad decision file on `main` bricks the gate for every item with no repair path through the gate. F5: a marker inside a fenced block is a live authorisation and the doc says the opposite — which matters because `instance/decisions/` is where verbatim operator words are stored. Both suites were green at the reviewed SHA: this is uncovered surface, not a false green. |
| V3-0.34 | **New 2026-08-04, at the operator's observation (Telegram 2113). The fleet floor is a constant where it should be a resource budget.** He noticed the host CPU saturated at five lanes and asked whether raising the floor to ten would help. Measured at that moment: 12 cores, load average 16.46, one `shellcheck` at 97% CPU inside a landing walk. It would not help — the lane count is not the scarce resource. The model does its thinking remotely; what consumes this host is the heavy tail of each lane: test suites, container builds, meteorite rebuild proofs, shellcheck. Adding lanes multiplies that tail against a fixed 12 cores, and the cost lands precisely on the thing we cannot afford to distort — **the suite is our measurement**, and under load it stretches (98s idle → 112.87s at six workers → 115.19s at twelve, where the deadline then killed a passing run: V3-0.23 OPEN-1). So the fleet gets slower and its evidence gets less trustworthy at the same time. The `floor 10` in the autonomy nudge is a number nobody derived, and the orchestrator has been quietly ignoring it all day while explaining why in prose — a rule that is routinely and correctly disobeyed should be replaced, not obeyed. | the fleet limit is derived from measured host capacity and the concurrency of the heavy steps rather than a hardcoded lane count; raising the lane count while the heavy steps queue does not degrade suite runtime, proven by measuring the suite at two fleet sizes; the nudge reports the derived limit and why |
| V3-0.40 | **New 2026-08-04, from the Fable global review (§1.15). A `verify:` line containing a pipe turns the lane-exit guard green on a red check.** `gate/completion-guard.ts:131` runs the lane's verify command with `spawnSync(command, { shell: true })`, and a shell pipeline exits with the status of its **last** command — so `verify: bun test \| tail -3` reports success whatever the suite did. Proven: `verify: exit 1 \| cat` gives `PASS verify-run` / `GUARD verdict=pass exit=0`, while `verify: exit 1` correctly gives `verdict=violation`. `verify-count:` would catch it, but that field is optional and only compared when present. **The two defects compose, and that is what makes this serious:** `parseVerificationCount` anchors on `^([0-9]+) pass$` while Bun indents its summary (V3-0.38), so `verify: bun test` can never satisfy `verify-count:` — and the natural fix an author reaches for is a `sed`/`awk` pipe, which is exactly the construction that discards the exit code. **The control that would catch the hole pushes authors into it.** Scope stated carefully: `gate/lane-exit.sh:67` calls the guard without `--defer-verify`, so the lane-exit choke point built by V3-0.17 and V3-0.5 is fooled and a lane can end `state: terminal` on a red suite; the landing path is not trivially fooled, because `gate/land.sh:285` defers verify and `land_run_declared_checks` uses a gate-owned baseline (V3-0.25). Hard Floor 7, and it reached this board only because an outside reviewer hit it while getting his own report past the guard. | a `verify:` line whose real check fails makes the guard refuse, whatever shell construction it uses — `verify: exit 1 \| cat` red before and after; `verify-count:` accepts the repository's own test command with Bun's leading whitespace; a lane cannot reach `state: terminal` on a failing suite | **open — top of the queue.** Fail-before evidence already exists in the Fable report; the fix is `pipefail` or a refusal of unguarded pipes, plus the `parseVerificationCount` anchor. Land with V3-0.38. |
| V3-0.41 | **New 2026-08-04, from the Fable global review (§1.8). The round counter's `check` command is a status query that is wrong for exactly the items that matter.** `bun gate/review-rounds.ts check --item-id V3-1.9` answers `admissible round=0` for an item that is genuinely parked, because `gate/land.sh:221` does `rm -f` on the cache and rebuilds it from the target branch, which only records items that **landed**. The park itself is sound and survives the wipe — replaying the durable attempt refs exactly as `gate/land.sh:264-281` does gives `round=1,2,3` and then `parked=no-progress` at the gate's own attempt — so the mechanism holds and the *interface* lies. Not cosmetic: V3-0.32 was filed partly on `check` output showing six live items at `round=0`. That row's underlying complaint stays correct (all six really do have zero attempt refs despite documented rejections), but anyone reading `check` as a status query is reading an answer that is wrong precisely for parked items. Also carries the two coverage findings the V3-0.15 reviewer left open: the executable-invocation lock only sees code blocks tagged `sh`/`bash`/`shell`/`console`, and a trailing backslash can desynchronise its fence tracker. | `check` either reconstructs from the durable attempt refs or refuses to answer outside a landing; asked about a parked item it never reports `admissible`; proven against V3-1.9 with the cache deleted | **open** — blocks trusting any evidence gathered with `check`, including V3-0.32's. |
| V3-0.42 | **New 2026-08-04, from the Fable global review (§3.6). The mechanism inventory against `v2-deprecated` is a snapshot with no checker.** This board's own closing section says so — "Phase 1's list is a snapshot that can go stale the same way the ledger did" — and the ledger got `tools/check-decision-ledger-drift.ts` while the mechanism inventory did not. Same drift class, already proven once on the ledger. | a tracked checker diffs the recorded mechanism inventory against `v2-deprecated` and fails closed when a mechanism exists there and is neither imported nor explicitly refused with a written reason; red-before evidence by removing one entry | **open** |
| V3-0.48 | **New 2026-08-04, from the V3-0.40 review. The count channel is silently disabled by two lines of ordinary output.** V3-0.40 made `verify-count:` mandatory when the command's own output reports a count — the right design, and the report correctly says it cannot be dodged by omitting the field. **It can be dodged by adding output.** The guard's two branches are asymmetric: an ambiguous count *with* a claim fails `command-output-missing-unambiguous-pass/fail-count`, while an ambiguous count *without* a claim falls through `if (claimedCount) fail(...)` at `gate/completion-guard.ts:317` and passes **with no message at all**. Measured at the reviewed SHA: `verify: bun test red.test.ts; printf " 0 pass\n 0 fail\n"` gives `GUARD verdict=pass exit=0` while the guard's own captured evidence contains ` 1 fail`. It needs no adversarial intent — an ordinary test that `console.log`s a summary-shaped line is enough, because the widened anchor now matches indented lines anywhere in the output. The reviewer judged it **not a landing blocker**, and was right: it is no worse than base, where the count channel did not exist, and holding the landing would have kept the live pipe hole in production. But it is Hard Floor 7 territory — a guard that returns pass while holding evidence of a failure. Also carries the reviewer's fifth finding: the two count policies still disagree on a non-zero fail total. | output containing count-shaped lines that cannot be resolved unambiguously makes the guard **refuse**, whether or not a `verify-count:` claim was made, and the refusal names what it could not resolve; a genuinely clean run with incidental summary-shaped log lines is still landable, or the row states why that trade is right; proven with the reviewer's two reproductions, red before and after | **open — fix next.** The fail-closed change is one branch. Land it before the next row depends on `verify-count` meaning anything. |
| V3-0.54 | **New 2026-08-04. `gate/land.sh` cannot be driven end to end without writing to the shared remote, so the only way to test the landing chain is to corrupt the state it measures.** `--no-push` fences only the `main` push. The review-rounds step pushes attempt refs to `origin` (`refs/bpa-review-attempts/…` and the mirrors) and then reads them back **from origin** to verify them, while `tools/check-github-ref-protection.sh` requires `origin` to be a GitHub URL — and `git remote get-url` expands `insteadOf`, so redirecting the transport locally breaks either the read-back or the suite. **There is no isolated mode.** Discovered the hard way: the V3-0.40 round-2 lane, trying to obtain evidence at the exact boundary its round 1 died at, ran the chain against the real remote and **consumed rounds two and three of the live V3-0.40 counter, ending at `REVIEW_ROUNDS status=fail parked=no-progress`**. It detected this itself, verified that the two attempt SHAs existed nowhere but its `/tmp` clone and were contained by no branch, deleted the four refs it had created, and disclosed it in its report's blocker line rather than burying it. `git ls-remote` now shows only the genuine round-one attempt; `origin/main` was never written. The orchestrator verified the remote independently: 21 attempt refs, no `V3-0.40` entry in the tracked counter. **The lane's handling was correct and is the standard** — detect, verify unreachability, reverse, disclose prominently. It remains the only case on this installation where a lane has removed durable refs, and it should stay so: the rule is unchanged, and it held here only because the refs were the lane's own pollution created minutes earlier. Same class again: **a harness writing into shared state that another mechanism reads as fact** — V3-0.20 was the ref namespace, V3-0.40 the measurement channel, this is the round counter. It is also *why* round 1 could not be proven at its boundary before landing. | the full landing chain including `post-merge-verify` runs against a disposable clone with attempt refs in a caller-supplied namespace or remote, and the GitHub-origin requirement decoupled from the transport; the real remote is byte-identical before and after, proven by comparing `git ls-remote` output; a genuinely stale or over-round lane is still refused in the isolated mode | **open.** Until it exists, the orchestrator is the only party that can exercise `post-merge-verify`, by landing for real and reading the verdict — which is what V3-0.40 round 2 asks for and is a legitimate use of the real gate. |
| V3-0.53 | **New 2026-08-04. The landing gate would land a change to its own evidence checkers without any review, because `gate/review-policy.conf` does not list `tools/`.** `gate/land-lib.sh` runs `tools/check-mechanism-reachability.test.ts` on every candidate, which makes that file evidence-gate code by any reading of `verification-and-locks` ("changes to the evidence gate require independent Tier-A review"). But the policy config lists `gate/` and not `tools/`, so `gate/land.sh` computes `LAND_REVIEW_REQUIRED=false` and the review step is skipped entirely. **Found because a lane refused to use it.** The V3-0.28 round-2 lane finished clean and landing-ready and then declined to land, writing that "landing it myself would have used that hole to skip the review of the very commit that documents it". That is the correct judgement and it is worth recording as the standard: a lane that can land itself unreviewed should decline and say so. The row exists so the next lane does not have to be that principled. Hard Rule 9 lists evidence-gate logic as requiring independent review; the configuration silently disagrees with the rule, which is the same two-halves shape as V3-0.50, this time between a policy sentence and the file that implements it. | a change touching any path whose contents the landing gate executes as evidence is review-required, proven by attempting to land a trivial edit to `tools/check-mechanism-reachability.ts` with no review artifact and finding it refused; the policy's path set is derived from what the gate actually runs rather than maintained by hand, or a test fails when the two diverge | **open.** Every checker the gate runs lives under `tools/` or `hygiene/`; check both, and check whether any other executed-evidence path is missing from the policy set. |
| V3-0.52 | **New 2026-08-04. A lane that correctly reports "the work is done, but landing needs something only the orchestrator can supply" makes its own row unlandable — and lanes disagree about which word to use, because `result:` is being asked to mean two different things.** `gate/completion-guard.ts:263,321` treats `result: NO-GO` as `GUARD verdict=no-go`, exit 3, and `gate/land.sh` refuses. There is no option anywhere in the gate to land a report that declares NO-GO. That is right when the *work* is not done. It is wrong when the work is complete and the only blocker is the independent review a coder lane is **forbidden** to write. **Observed on the same day, in the same situation, with opposite answers**: V3-0.30 round 5 wrote `result: NO-GO` with the explicit gloss "`NO-GO` is the classification of the LANDING, not of the work"; V3-2.9 wrote `result: NO-GO` with the missing review artifact as its blocker; V3-0.29 round 4 wrote `result: clean` with `remaining: F7 needs an operator decision`. Two of the three then could not be landed after their reviews came back **ACCEPT** — V3-0.30's escalated review refused eleven forgery mutations and still could not reach `main`, because a word written before the review existed outlived the condition it described. The cost is a whole extra lane round per row purely to re-issue a report, which is what the orchestrator is now doing twice. Same family as V3-0.50: one field, two readings, no shared definition. | the terminal report can express "work complete, landing blocked on an artifact this lane may not produce" in a way the gate accepts once that artifact exists, without a second lane round; a report whose work is genuinely incomplete still refuses; proven by driving one row from review-pending to landed with no re-issue, and by confirming an incomplete row cannot use the same path | **open.** Two rows are blocked on it right now (V3-0.30 round 5 and V3-2.9), both with completed, verified work. Cheap, and it removes a whole round from every Tier-A row's cost — which is exactly the tax the sprint-05 planning is about. |
| V3-0.51 | **New 2026-08-04, found by a consilium member and independently reproduced by the orchestrator. Every lane in the fleet runs the test suite against a 120-second kill it does not know exists — and this, not the suite, is what V3-0.23 has been chasing for four rounds.** Measured: a foreground command in the lane agent harness is killed at **exactly 2m00s**, returns **exit 143**, and returns its partial output **with no trailing lines**. The orchestrator reproduced it directly: 130 numbered lines plus a summary, killed at 2m00s, output ends at line 120, no summary emitted. Against that, the suite's measured runtime across **nine independent runs by four different agents** is **98-155 seconds** (98s/112.87s/115.19s at 0/6/12 workers; 133s and 126s; Fable's 129s/121s/126s; two guard-driven runs at 154.83s and 122.53s). **The distribution straddles the axe.** And nothing configures the bound: `grep -rn "BASH_DEFAULT_TIMEOUT_MS" .` returns nothing, and `instance/lane-agent-command.conf` launches the agent with no timeout setting. **The composition is the whole of V3-0.23's symptom.** The kill gives 143; 143 through an unguarded pipe becomes 0 (V3-0.40, and `gate/completion-guard.ts:131` uses `spawnSync(command, {shell: true})`, which returns the pipeline's last status). So a lane sees exit 0, truncated output, and no trailing triple — which is verbatim what V3-0.23 records. It also explains why the defect was never reproduced by anyone measuring from outside a lane: the Fable review's five runs and round 2's reviewer all ran the suite in their own shell, under the axe. **The orchestrator's own briefs made it worse**: they instructed lanes to "measure in the foreground under `timeout`", which is precisely the shape that gets killed. That advice is in most briefs written today and must be corrected wherever it appears. | a lane can run the full suite to completion and obtain a trailing triple, proven by a lane that runs it at a time when the suite takes more than 120 seconds; the bound the lane runs under is declared in tracked configuration rather than inherited silently; a command that genuinely exceeds the declared bound is still killed and the lane reports the kill as a kill, never as a pass | **open — highest value on the board.** It is cheap, it explains four rounds of work on V3-0.23, and it is currently corrupting the evidence of every lane whose suite run crosses two minutes. V3-0.23's own work is not wasted: its round-2 reviewer proved by arithmetic that the stall watchdog decides later than the runner containing it, which is a real defect on its own merits — but the row's premise belongs here. |
| V3-0.50 | **New 2026-08-04, raised by a consilium member against the orchestrator's own triage. The recurring defect class has never itself been a row, so each new instance is dispatched as if it were a surprise.** Six instances are on this board, every one found today, every one the same shape — a contract whose two sides were written separately and never executed against each other: V3-0.31 (the lane report has two names), V3-0.38 (the count field cannot match Bun's output), V3-0.39 (the review artifact's required fields live only inside the function that parses them), V3-0.40 (the verify command's exit status and the count channel disagree), V3-0.43 (the workboard row shape has no reader), V3-0.44 (the lane-exit guard has no reviewer contract). **The criticism that stings and is correct**: V3-0.44's own text says "fourth instance" — the orchestrator wrote that sentence and then filed the fifth and sixth in exactly the same way, one narrow row each, without ever making the pattern the thing to fix. The proposal is a single audit: enumerate every declared two-sided contract in this repository — report shape, review-artifact shape, verify-command shape, workboard-row shape, lane-exit role shape — and give each **one shared schema plus a round-trip test**, instead of a parser per side. The member judged this would likely have caught at least four of the six in one pass. Its own recommendation, which this row adopts: do **not** re-scope the narrow fixes already in flight — stopping now wastes real work — but file the audit **before the seventh instance appears**, rather than after, since the seventh is very likely already coming. | every declared two-sided contract in the repository is enumerated in a tracked file; each has one shared definition that both the producer and the consumer read, and a round-trip test that generates from the definition and parses with the real consumer; adding a contract without a round-trip test fails closed; proven by removing one side's field and finding the round-trip test red before the parser is even reached | **open — file now, work after the six in flight land.** The scoping risk is real and is named: generalising too early turns a narrow proof fight into a design fight, which is how V3-0.29 reached round 4. Keep the audit's first deliverable to the enumeration and one round-trip test, and let HR-2166's ladder handle the rest. |
| V3-0.49 | **New 2026-08-04, from the V3-0.40 review. Three pre-existing holes in the completion guard's own robustness, one of which is quietly eating the worktree budget.** All three are identical at base, outside V3-0.40's diff, and each defeats a property the guard is supposed to have. **(a) A report with no verify command at all passes.** `gate/report-contract.ts` `lineValue` builds `^verify:\s*(.*)$`, and JavaScript's `\s` matches newlines, so a bare `verify:` line takes **the following line of the report** as its command — `verify:` followed by `true` gives `PASS verify present`, `PASS verify-run`, `GUARD verdict=pass`. `fail("verify", "empty")` at `completion-guard.ts:293` is therefore unreachable, and the "an absent verify command must refuse" property does not hold. **(b) A hanging verify command hangs the guard and leaks a worktree.** `spawnSync` at `completion-guard.ts:148` has no `timeout`; `verify: sleep 600` was still running at 20s, and killing it skips the `finally`, so the `completion-verify-*` worktree stays registered. Not hypothetical: `git worktree list` in the subject repository shows **18 stale `completion-verify-*` entries**, one of them at `8833c92`. That is a direct contributor to V3-0.46 — the worktree count that pins every branch. **(c) A report path that is a directory crashes the guard** with an uncaught `EISDIR` at `completion-guard.ts:233`, exit 1, a stack trace and no `GUARD` line. Fail-closed in effect, because both callers treat non-zero as blocked, but it never names the reason. | a report with an empty `verify:` value refuses and says so; a verify command that exceeds a stated bound is killed, the guard names the timeout, and its temporary worktree is removed on every exit path including the kill; a report path that is a directory or unreadable produces a named refusal rather than a stack trace; the 18 stale `completion-verify-*` worktrees are accounted for — removed or explained | **open.** (b) should be done with V3-0.46, since they are the same budget seen from two ends. |
| V3-0.47 | **New 2026-08-04. The orchestrator cannot record anything without invalidating work in flight, and it cost three interruptions in one day.** The orchestrator writes bookkeeping — board rows, decision files, review-item registrations — directly to `origin/main`. The landing gate's `freshness` step and its `push` step both require the target not to have moved. The two are individually correct and jointly unusable, which is the two-writers-on-shared-git-state class again. Measured today: (1) the V3-0.15 landing had to be preceded by a manual fast-forward of `land-main`; (2) the V3-1.9a landing ran the full ten-minute chain, passed merge and meteorite, then **aborted at `push`** because the orchestrator had pushed `055be5d` after the gate started — the rollback was clean, but the work was repeated; (3) the V3-0.29 round-4 lane's own landing attempt died at `LAND step=freshness status=fail` because `origin/main` moved from `ddbd474` to `20c4aab` while it ran. The third case is the instructive one: nothing was consumed, because freshness is checked long before the attempt ref is pushed — the ordering that protects it is the same ordering V3-0.29's F2 is about. So the gate is not wrong; the orchestrator's write pattern is. Note also the shape of the workaround in use today: the orchestrator hand-fast-forwards a shared checkout between landings, which is exactly the fragile human-operated coordination this repository exists to replace. | the orchestrator can record bookkeeping while a landing is in flight without aborting it, and without a hand-run fast-forward of a shared checkout; proven by pushing a bookkeeping commit at each stage of a landing — before freshness, between merge and push — and finding the landing completes or refuses for a stated reason that is not the orchestrator's own write; a genuinely stale lane is still refused | **open.** Two candidate shapes, and the choice should be argued rather than assumed: serialise bookkeeping behind the same lock the gate uses, or let the gate tolerate a target that moved when the move is content-neutral for the paths it guards — the round-4 lane demonstrated that test (`git diff --stat <base>..origin/main -- gate/ instructions/review-policy.md` empty) is cheap and decidable. |
| V3-0.46 | **New 2026-08-04. The reaper is correct and cannot free anything, because every branch is pinned by a worktree nobody removes.** Measured on this installation: 108 lane branches, 120 worktrees, 87 entries in `instance/hygiene-protected-branches.txt`. Running `hygiene/reap.sh branches` in its default dry-run mode shows the mechanism working exactly as designed — `safety=pass carried-by=48a59b3` on branches whose work is on main, `safety=refused detail=unique-content` on the rest, and for the great majority `held by live worktree, refusing`. It refuses rather than guessing, which is right. `hygiene/reap.sh worktrees` reports **no orphaned worktrees**, because none of them is orphaned: each is a registered worktree of a lane that finished without landing. So the two commands are each correct and together free nothing. **Two separate causes, and only the second is deferred.** (1) A lane that ends without landing leaves its worktree registered forever; only `gate/land.sh --worktree` removes one, and only on a successful landing (see V3-0.33, where omitting the flag produced `landed-reap-failed`). (2) Nothing is armed on this host — `crontab -l` says `no crontab for root`, while `hygiene/install-cron.sh` would schedule the three sweeps hourly. Cause 2 is HR-1720 working as intended, host deployment deferred to v3. Cause 1 is a real gap and is not deferred: even fully armed, the reaper would free 15 of 108 branches today. This is also a live instance of the class V3-0.28 was reopened for — a mechanism written, tested, given an installer, and never armed. | a lane that terminates without landing has its worktree released, so the reaper can decide its branch on the merit of the work rather than on a pin; proven by driving a lane to a non-landing terminal state and finding the worktree gone and the branch classified; committed work is never destroyed by the release — a branch carrying unique content still survives the sweep, which the dry-run above already demonstrates | **open.** Steady state should be bounded by the fleet, not by history: roughly two branches per row in flight, so about 25-30 at a fleet of ten, against 108 today. Do not treat the 87 protected entries as breeding — that is retention by policy and is working. |
| V3-0.45 | **New 2026-08-04, operator ruling HR-2166. Between the review cap and the park there must be a proving phase.** Today HR-1726 caps review at three rounds and the row parks. The operator's ruling inserts up to three further rounds whose purpose is **proof, not redesign** — run the tests, produce debug logs, reproduce the reviewer's finding on the lane's own SHA — the same practice a product coder follows, where the person who wrote the change demonstrates it works. Only after that does the row park and come to him, and he explicitly confirmed the orchestrator should keep bringing it rather than finishing the work itself. The evidence says he is right: every rejection recorded on this installation so far was for **missing or unexecuted evidence, not a wrong idea** — V3-0.15 r1 (a documented command that could not run as written), V3-0.20 r1 (one measurement unreproducible out of seven findings), V3-0.23 r1 and r2 (locks running at a budget where the defect was arithmetically impossible), V3-0.29 r3 (four blocking findings outside what the locks assert, both suites green), V3-1.9a r1 and r2 (the destructive path never executed against a real account). In each the design survived and the evidence did not, so rounds spent proving are aimed at the actual failure mode and are cheaper than a recut. Verbatim ruling in `instance/decisions/HR-2166.md`. | a row that has exhausted its review rounds enters a proving phase rather than parking; a proving round that changes the decision under review is refused as a redesign; a proving round that produces logs without a pass is still not a pass (Hard Floor 7); the phase is bounded and the row still parks and escalates at its end; proven by driving a fixture row through review-cap, proving rounds and park, and by attacking it with a proving round that edits the subject | **open** — depends on V3-0.32 (rounds are charged only at landing, so the counter cannot see the rejections a proving phase must count) and on V3-0.41 (`check` reports the cache, not the truth). V3-0.44 matters too: today "did the proving round finish" cannot be read from lane status, because every reviewer lane reports `failed`. |
| V3-0.44 | **New 2026-08-04. The lane-exit guard has no reviewer contract, so every reviewer lane in the system reports `failed`.** `gate/lane-exit.sh` takes `--report`, `--repo` and `--branch` and never learns the lane's role, so it applies the coder report contract to every lane. A reviewer by design commits nothing and writes a `.review.md` carrying `verdict:`, `reviewer:`, `reviewed-sha:` and `independence:` — a shape `gate/completion-guard.ts` cannot accept — so it can never exit 0. Measured across every reviewer lane on this installation: **14 of 14 ended `state: failed reason: report-invalid`**, including `lane-v3-1.9a-provisioner-rev3b` and `lane-v3-0.15-documented-cli-rev3`, whose ACCEPTs both landed. The signal is therefore 100% false for half the fleet, and it fails in the worst direction: the guard exists precisely to catch a lane that ended without usable work — its own header records that of 1314 unmerged branches measured, exactly 6 satisfied the landing gate's precondition — and for reviewers it fires always, so it distinguishes nothing. This compounds V3-0.37: when a reviewer dies mid-measurement, the one status that would say so is already saturated, and the orchestrator must open the artifact by hand to learn whether the review happened. Fourth instance in two days of one class — a contract whose two halves were written separately and never executed against each other (V3-0.31, V3-0.38, V3-0.39). | a reviewer lane that produced a contract-valid review artifact exits 0 and ends `state: terminal`; a reviewer lane that produced no artifact, or a malformed one, still fails closed and names what is missing; proven by running `gate/lane-exit.sh` against a real ACCEPT artifact and against a truncated one, not against a hand-written fixture | **REOPENED 2026-08-04 as r2 — round 1 met this row's acceptance and did not fix the defect.** `6714091` landed the `--role` plumbing and `gate/lane-exit.test.sh` proves the guard honours a reviewer contract, so the acceptance command above genuinely exits 0. Lanes still all reported `failed`, because the cause was never in either half of the contract: **systemd expands the `ExecStart` line before bash sees it**, and `${10}`, `${PIPESTATUS[@]}` and `${pipeline_status[N]}` are not valid environment variable names, so systemd substituted an empty string for each and announced it in the unit journal on every launch (`Invalid environment variable name evaluates to an empty string: 10, PIPESTATUS[@], …`). Unbraced `$1`…`$9` survived, so the launcher read as correct. `role` was the tenth positional parameter, hence `--role ""`, which `gate/lane-exit.sh` rejects as a usage error (exit 2) **before running any check** — recorded as `report-invalid` regardless of the work's quality. The silent half was worse: with `pipeline_status` empty, `((agent_status != 0))` is a do-nothing no-op, so the crashed-agent and failed-masker checks were disabled outright — measured against the pre-fix body, an agent exiting 17 produced `state: terminal reason: report-valid exit: 0`, i.e. a dead agent reported as a clean lane. Both halves of the contract were correct in isolation and the transport between them was assumed lossless; that assumption is the actual defect class here, not the role flag. **Added acceptance for r2**: the lane wrapper is a file (`orchestrator/fleet/lane-payload.sh`) that systemd never parses, the launcher refuses any lane value containing `$` rather than letting it be emptied in transit, and the lock crosses the real boundary — `orchestrator/fleet/lane-payload-systemd.test.sh` launches through a real `systemd-run`, asserts a non-empty role reaches the gate and that a crashed agent and a failed masker are reported as `payload-exit`/`log-masker-exit`, and skips by name (`EXCLUDED … capability=systemd-transient-unit`, pinned in `instance/expected-shell-capability-exclusions.tsv`) where systemd is unavailable, never silently. Note for anyone reading old evidence: lane status files written before r2 are untrustworthy wholesale — 59 `failed` / 36 `terminal` on this host, and several of the `failed` lanes landed good work. Write-up: `instance/incidents/2026-08-04-systemd-ate-the-tenth-argument.md`. |
| V3-0.43 | **New 2026-08-04, found by the orchestrator while filing the rows above. Nothing checks the workboard itself, and the orchestrator silently committed a corrupted one.** `e0cd52b` ("file V3-0.38") inserted the intended row **plus a duplicate of the document header and the whole of Phase 0 and Phase 1** — 66 spurious lines — and it passed the landing gate, because no check reads `instance/workboard.md`. **Root cause, reproduced:** the filing script used `text.replace(anchor, rowText)` with a *string* replacement, and JavaScript expands `$&`, `` $` `` and `$'` inside a string replacement. The V3-0.38 row quotes the regex `` `^([0-9]+) pass$` `` — a `$` immediately followed by a backtick — so `` $` `` expanded to *the entire document before the match* and pasted it in. The row describing an anchoring bug corrupted the board through an anchoring bug. Fix in the filing tool is a function replacement, which disables the patterns. The damage then compounded: later insertions used first-match anchors, so V3-0.39 went into the **stale** copy while the state updates went into the live one, and the two copies diverged by 19 rows before anyone looked. Repaired by restoring `c0c0609` and re-applying each intended change, which is why V3-0.38 and V3-0.39 are re-stated in this commit. **A second, older defect surfaced while checking the repair:** of 80 rows, **25 carry only three cells and have no state column at all** (V3-0.19, V3-0.23, V3-0.24, V3-0.26, V3-0.31 through V3-0.37, V3-1.2, V3-1.10, V3-1.11, V3-2.1, V3-2.3, V3-3.1, V3-3.2, V3-3.3, V3-3.5, V3-3.7, V3-4.2, V3-4.3, and — missed by the orchestrator's first audit because its regex ignored letter-suffixed ids — V3-1.9a and V3-1.9b), and 6 more split into extra cells because their prose contains an unescaped pipe. So a quarter of the plan renders wrong and cannot be read for status — including V3-0.23, the row the whole critical path waits on. None of this is inventable back: a missing state must be re-derived from landing evidence, not guessed. This is the plan artifact the operator reads and every dispatch decision is made from — the same document the Fable review independently called "not evidence". | a tracked checker fails closed on a workboard with a duplicated section, a duplicated row id, or a row whose column count is wrong, and runs in the landing tier; red-before evidence is the exact damage `e0cd52b` introduced, replayed as a fixture | **open** — cheap, and it is the guard that would have caught today's damage on the commit that caused it. |
| V3-2.9 | **New 2026-08-04, from the Fable global review (§3.1). Nothing enumerates the host state that is not in git, and nothing restores it — Hard Floor 5 is discharged only for the repository.** Zero hits for "backup" across all 69 rows. The meteorite proves the *repository* rebuilds a host; it does not preserve the SQLite state DB, mission and lane history, or the durable evidence under `$XDG_STATE_HOME`. `instructions/reproducible-from-git.md` already requires host state that must not be in git to be **enumerated** — "not in git is never allowed to mean not written down" — and no such enumeration exists. This sits directly under the operator's "reboot the whole machine" milestone: he can rebuild the machine correctly and still lose the record of what it was doing. | every piece of non-git host state is enumerated in a tracked file with the command that verifies each; a restore path exists and is proven by restoring into the meteorite container and finding the mission and lane history intact | **open — the Fable review ranks this the largest gap on the board.** Blocks V3-4.2 being meaningful: a rebuild that loses the system's memory is not a rebuild. |
| V3-2.10 | **New 2026-08-04, from the Fable global review (§3.2). Credential provisioning and rotation is not a written, verified procedure.** Zero hits for "rotation". Provider credentials are interactive by design and correctly excluded from git, but nothing enumerates what they are, where they go, what permissions each needs, and the command that verifies it — which `reproducible-from-git` requires by name. On a rebuild this is the step that stops everything, and it is exactly the place where an untested runbook reads like a tested one. | a tracked runbook names every credential, its destination, its required scope and a verification command that exits non-zero when the credential is absent or wrong; the runbook is executed once end to end against a fixture identity, with no secret entering git | **open** — pairs with V3-2.9; both are existing meteorite obligations, not new scope. |
| V3-2.11 | **New 2026-08-04. The autonomy watchdog died, and the mechanism that would have caught it was written, tested, and then dropped in the v2→v3 migration.** `orch-fleet-nudge` began failing every ten minutes at ~17:53 with `refusing to run with an unparseable workboard`, so nothing woke the orchestrator when the fleet went idle and nothing told the operator it had stopped — he found out today by noticing. Cause: the deployed script parses v2's bullet board (`- <!-- status: open --> **ID —`), while v3's board is a table; it had been reading the **deprecated** board for 29 hours (the repo sat checked out on `v2-deprecated` from 08-03 12:40 to 08-04 17:52, reporting a constant `open_rows=47`) and broke the moment the tree returned to `main`. Three compounding facts: `v2-deprecated` carries the whole mechanism tracked and tested — script, tests, service, timer — **plus `fleet-nudge-liveness`, a watchdog for the watchdog, with its own units and tests**, none of it ported to v3; the copy running on the host is **not** that file (120 lines against v2's 141), hand-edited in place so the deployed copy silently diverged from its source; and the script has carried a `--verify-deployed` flag built to detect exactly that divergence, which nothing ever invoked. This is the shape the operator predicted in Telegram 1767 — "whisper was in the requirements, implemented on v2 and v3, and we still missed it… there will be more such misses". | the watchdog runs from a **tracked** copy, parses the v3 table, keeps its fail-closed behaviour (an unreadable board must never read as "no work"), defaults to the HR-2342 cap of three rather than ten, and its deployed-vs-tracked drift is reported rather than assumed; proven by the liveness alarm detecting a stopped watchdog and a drifted deployed copy — **superseded in part by the Tier-A consilium, 2026-08-04 evening**: the units now run the tracked script straight out of `${INSTALL_ROOT}`, so there is no deployed copy to drift from and the drift channel is deleted rather than maintained; the drift half of this criterion is replaced by "a clean clone yields a registered, runnable mechanism whose `ExecStart` is checked by `bootstrap/check-unit-drift.sh`". Added by the same consilium: every operator notification on the timer path deduplicates and clears on repair, and the liveness alarm reads the heartbeat's `status=` so a watchdog that fires and fails every time is detected rather than reported green | **round 2 in review on `ag-v3-2.11-r2`**. Round 1 (`a2f7e90`) was ACCEPTed by lens one and REJECTed by lens two on B1–B4; this cut fixes all four plus O6/O7. Lens two's central finding: tonight the storm WAS the detector — the harm channel and the detection channel were the same channel, so B1 (deduplicate) and B2 (read `status=`) had to land together or the system gets worse in one direction or the other. Measured over 36 firings (one night of a ten-minute timer), each operator path fell 36 messages -> 1, and a fresh `status=2` heartbeat now takes the alarm from exit 0 / 0 messages to exit 1 / exactly 1. B3: the HR-2342 cap is no longer installed as a floor — 1 and 2 lanes are silent (they nudged the orchestrator 36×/night before), 0 lanes is the only fault, and `CRITICAL` is clamped to at least 1 so the severe tier cannot become unreachable; the derived budget stays V3-0.34 and the target knob is off by default rather than carrying another underived constant. B4: the four units are `bootstrap/units/*.in` templates anchored at `${INSTALL_ROOT}` like every other unit in the repo, registered in `expected-units.tsv`, `expected-mechanisms.tsv` and `required-mechanisms.tsv` with exclusion rows — the previous lane's stated blocker ("registration needs an arm edge only deployment provides") is refuted: all 17 pre-existing units register unarmed the same way. **The timer stays STOPPED**; deploying is a separate, daylight step. |
| V3-1.12 | **New 2026-08-04, from the Fable global review (§3.4). Three critical-path rows block on one person, with no timeout, fallback or safe-idle behaviour defined.** V3-1.9 is parked pending an operator action, V3-1.7 awaits his restatement, V3-4.3 needs his explicit go. Zero hits for operator-absence handling anywhere on the board. HR-2109's administrator bot improves *reachability*, not *absence* — a bot he does not read blocks exactly as hard. What the fleet should do after a bounded wait is undefined, so today it would either idle silently or keep dispatching into rows it cannot close. | a blocked-on-operator row has a defined bounded wait and a defined safe state at expiry; the fleet parks such rows and continues on autonomous work rather than idling or looping, proven by simulating a request that is never answered | **open** — the operator has stated he is not always at a computer (Telegram 2103), so this is a stated operating condition, not a hypothetical. |
| V3-3.9 | **New 2026-08-04, from the Fable global review (§3.3). Provider quota exhaustion mid-lane is a work-loss event and nothing defines it.** V3-3.6 *displays* quota; nothing says what happens when it reaches zero with lanes running. At a fleet of five to ten, a mid-flight exhaustion produces a wave of lanes that stop without terminal reports — and V3-0.37 has now shown that makes their committed work unlandable. Observed live today: Spark exhaustion is what ended the Codex tier and drove the move of all load to Anthropic. | quota exhaustion during a lane is detected and the lane writes a terminal report naming it, so its committed work stays landable; the fleet degrades to the remaining providers instead of dispatching into a dead one; proven by simulating a quota-exhausted provider response | **open** — depends on V3-0.37 to be worth anything. |
| V3-4.4 | **New 2026-08-04, from the Fable global review (§3.5). The Telegram daemon is the only channel and only one of its failure modes is covered.** V3-4.1 proves it survives a restart; nothing covers the token being revoked, Telegram being unreachable, or the daemon wedging while the orchestrator is healthy. Measured on this host today: `bpa-orchestrator.service` loaded but inactive; the watchdog service and timer, the meteorite timer and the deploy-drift timer all `not-found`; `no crontab for root`. HR-1720 defers host deployment, so absence is partly in-scope-deferred and V3-2.1/V3-2.3 are correctly still open — but the orchestrator has no supervision running here at all, and `no crontab for root` is verbatim the string V3-0.28's row cites as instance #2 of the class it was closed on. | a wedged or unreachable daemon is detected by something that is not the daemon, and the operator learns of it through a path that does not depend on the daemon; each of the three failure modes has an executed test | **open** — decide first which of the six units are supposed to be running on this host today, given HR-1720. |
| V3-0.39 | **New 2026-08-04. The review artifact has no single home either, and an ACCEPT was thrown away because of it.** `land_review_check` (`gate/land-lib.sh:473-484`) requires exactly one `reviewer:` line, plus `reviewed-sha:` and `independence:`, and exactly one `verdict:`. Nothing outside that function states the contract, so the orchestrator's review briefs asked for "your identity" and reviewers wrote `identity:`. Result: a 481-line ACCEPT for V3-1.9a, with nine findings closed by execution including the destructive `state=creating` defect, was refused at `LAND step=review status=fail … malformed-artifact` — evidence discarded over a field name the reviewer was never told. Third instance of one class that day: V3-0.31 (report path named twice), V3-0.38 (count field that cannot match), and this. Each is a contract whose two halves were written separately and never executed against each other, and each cost real review rounds. | the review artifact's required fields have one tracked home that both the brief and the gate read; a reviewer following the documented shape produces an artifact the gate accepts, proven by generating one from the documented template and running `land_review_check` against it; a malformed artifact still fails closed and names the missing field | **open** — worked around for now by a hand-written exact-field-names block in each review brief, which is precisely the prose-not-mechanism shape V3-0.31 is about. |
| V3-0.38 | **New 2026-08-04. The report contract's structured count field can never validate, because Bun indents its summary.** `gate/completion-guard.ts` rejects a narrated test-count claim and directs it into the structured `verify-count` field — which `parseVerificationCount` then validates against `^([0-9]+) pass$`. Bun emits ` 560 pass` with a leading space, so the anchor never matches and the field the contract insists on is unusable. Found independently by two lanes on the same day (V3-0.15 round 3 hit it in its own regex; the V3-0.23 reporting lane executed the guard's exact pattern against a real suite log), and a third time by the Fable global review, which additionally showed the natural workaround is itself a hole (V3-0.40). The consequence is quiet and bad: the contract pushes lanes toward a structured field, the field silently fails, and lanes drift back to narrating counts in prose — which is the habit that let a `sed`+`printf` verify line report a red suite as green earlier today. Same root as V3-0.31: a mechanism whose two halves were specified by different people and never executed against each other. | a lane that reports a real Bun triple in `verify-count` passes the guard; a lane that reports a count the suite did not produce still fails; proven by running the guard against real suite output rather than a hand-written fixture | **open** — fix together with V3-0.40; they are two halves of one defect. |
| V3-0.37 | **New 2026-08-04. A lane that ends its turn while a measurement is still running loses everything it did.** `ag-s10-8-r2` committed `cd2890d` (754 lines rewriting the shell-tier guard), then ended with the words *"Still running under load. Waiting for the notification."* — no terminal report, so `gate/lane-exit.sh` correctly refused with `FAIL report-file missing` and the work could not land. A continuation lane dispatched to finish it did **exactly the same thing**. The trigger is structural, not carelessness: this row's own measurement takes ~245s under deliberate load, long enough that a lane starts it, has nothing to say, and stops. The guard behaves correctly — work without evidence must not land — but the outcome is that the longest, most valuable measurements are the ones most likely to be discarded, which inverts the incentive the evidence gate exists to create. Seen on at least eight lanes today. | a lane that has committed work but produced no report is detected and given the chance to finish, or the launcher re-invokes it once with the missing-report reason, instead of the work being silently unlandable; proven by executing a lane that exits mid-measurement and observing the recovery |
| V3-0.36 | **New 2026-08-04, HR-2126. Land with squash, and let the durable refs keep the real history.** The operator asked for two things at once: lanes commit and push often enough that a reboot cannot lose unlanded work (which is also the precondition for putting worktrees on tmpfs, V3-1.11), and landings squash into `main` so the default branch does not accumulate a lane's intermediate commits. The second is usually a trade — clean history against lost provenance — but here it is not: every landing already publishes the exact lane tip to `refs/bpa-review-attempts/<ns>/N-<sha>` and its mirror on origin, so the lane's real history survives independently of what `main` records. Squashing therefore costs no evidence, and the attempt ref becomes the authoritative link from a one-line `main` commit back to how it was reached. Care is needed where the chain is checked: the report's `commit:` field, the branch-tip guard, the meteorite proof and the round counter all name the pre-squash SHA today. | a landing produces exactly one commit on `main`; the lane's original SHA remains reachable via its attempt ref and the landed commit names it; every existing gate step still binds the same evidence chain, proven by landing a multi-commit lane and following the chain back from `main` to the lane tip; lanes push each commit so no unlanded work exists only on this host |
| V3-0.35 | **New 2026-08-04, HR-2115. Lane names carry no meaning, so the operator cannot tell what is running.** He was watching `btop`, saw a dozen `s10-8-r2-measure.sh` processes and had to ask what they were — they were that lane's deliberate load generator, reproducing the deadline defect. Lane names today are dispatch-order serials (`s10-8-r2`) chosen by the orchestrator and meaningful only to it. Everything downstream inherits them: worktree paths, branch names, log files, process titles, the autonomy notifications he reads. His ask: name lanes so he can tell them apart by sense and tie each to its row. Note this is not cosmetic — the operator watching the machine is a real observer of this system, and today he cannot answer 'what is this process' without asking the orchestrator, which is the same dependency on a single agent that the rest of v3 exists to remove. | a lane name derives from its work item and a short slug (`v3-0.23-suite-deadline-r2`), is visible in the process title, worktree path, branch, log filename and every notification, and is unique per round; a reader with only `btop` open can map any lane process back to its workboard row |
| V3-0.33 | **New 2026-08-04. The reap step cannot reap a branch whose worktree the gate was not told about.** `gate/land.sh` merged, pushed and verified `ag-s10-3-r2` at `cd0b1c3`, then failed to delete the branch: `error: cannot delete branch ag-s10-3-r2 used by worktree at /root/.cache/infra-lanes/s10-3-r2`, giving `verdict=landed-reap-failed`. The gate accepts an optional `--worktree <path>`; the orchestrator did not pass it, so a correct landing ended in a state Hard Floor 1 forbids — a merged lane branch and its worktree both still alive — and only a human noticing the verdict cleaned it up. Git already knows which worktree holds a branch (it is the text of the error), so the information the caller forgot is information the gate can enumerate. A flag that must be remembered is a mechanism that will be forgotten. | the gate discovers the worktree holding the candidate branch and reaps both, or refuses **before** merging with a named reason; a landing without `--worktree` leaves no branch and no worktree behind, proven by executing one |
| V3-0.32 | **New 2026-08-04, at the operator's question (Telegram 2093). The three-round cap bounds nothing, because a rejection is not a round.** `gate/land.sh` charges an attempt at landing time, so a row that is REJECTed by review and never reaches the gate stays at `round=0` forever. Verified on six live items — V3-0.15, V3-0.20, V3-0.29, V3-0.30, V3-1.9a and V3-4.1 all report `admissible round=0` after between one and three independent review rejections each. The consequence cuts both ways: no review rejection can ever park a row, so HR-1726's cap is enforced only by the orchestrator remembering to apply it, which is exactly the kind of human-operated coordination this repository exists to replace; and conversely the counter's `noProgress` signal cannot see the work that produced no progress. The operator noticed this from a passing remark in a status report and asked what would be done about it. | a review verdict charges a round; a row rejected three times parks without the orchestrator deciding to, proven by executing three rejections against a fixture item; landing still charges exactly once, so a row cannot be double-counted for the same round |
| V3-0.31 | **New 2026-08-04. The lane terminal report has two names, and the guard checks the one nobody writes.** `orchestrator/fleet/launch-lane.sh:77` derives `report="$lanes_dir/$name.report.md"`, exports it as `LANE_REPORT_PATH`, and `gate/lane-exit.sh` is invoked against exactly that path (`:156`). Every orchestrator brief instead tells the lane to write `<branch>.report.md` — and lanes obey the brief. Result: a lane that did everything right terminates `state: failed reason: report-invalid`, and the dispatcher's terminal-evidence path is dead in practice because the orchestrator reads the branch-named file by hand. Confirmed on `s10-1`: `state: failed` at `s10-1.report.md` (absent) while `ag-s10-1.report.md` passes the guard on every check. It has cost real rounds — both reviewers of V3-0.16 REJECTed for exactly this ("the required coder terminal report `/root/.cache/infra-lanes/ag-s5-9-r2.report.md` does not exist"), which is two wasted review rounds on a naming collision. The orchestrator's briefs are the drift; the launcher is the mechanism. | one home for the report path: the brief tells the lane to write `$LANE_REPORT_PATH` and nothing else names the file; a test proves a lane that writes the exported path terminates `state: terminal`, and a lane that writes any other path does not |
| V3-0.30 | **New 2026-08-04. Nothing reconciles the workboard against what actually landed, so finished work is dispatched again.** V3-0.16 landed at `39a7698` and its row stayed open; the orchestrator read the row, read two REJECT verdicts in the lane logs (superseded by a later ACCEPT it did not find), wrote a full brief citing a commit "on no branch", and launched a lane onto a closed row. The lane was stopped after ~12 minutes and its branch reaped. Three independent sources disagreed and none of them checked the others: the workboard said open, `.git/bpa-review-rounds.json` said `landed=39a76988`, and `origin/main` contained the files. This is the repository's dominant shape — **evidence pointing one step away from the property it claims** — applied to the dispatch decision itself, and it wastes a lane every time it fires. It also makes the "47 open rows" figure in the fleet nudge untrustworthy, which is the number the orchestrator uses to decide what to do next. | a check compares every open workboard row against the landed record and `origin/main`, and is red when a row is open whose item the counter records as landed; the fleet nudge's open-row count is derived from it | **round 4 REJECTed 2026-08-04 — and the cause is the orchestrator's brief, not the lane.** Round 4 closed the round-3 blocking finding as instructed: it inverted the model so that landed evidence decides. The instruction was too absolute. The reviewer's blocking finding is that **an item that has ever landed can never be dispatched again** — and the live board has exactly that case today: V3-0.28, which the orchestrator reopened this morning on the Fable review. A guard built from "landed means refuse" cannot express a legitimately reopened row, so it would now block the fix for the defect that reopened it. Round 5 must state the model that covers both: landed evidence refuses a dispatch **unless** the row records a reopening backed by evidence, and a reopening is itself a claim that must be checkable rather than a word in a cell. |
| V3-0.28 | **New 2026-08-04. A tracked mechanism with no executor is this repository's most common defect, and nothing detects it.** Five instances, all real, all found only when something else went wrong: `orch-runtime-watchdog.timer` installed and never armed (the 2026-08-01 incident, unnoticed for days); the reap cron tracked and never invoked — `crontab -l` → `no crontab for root` — while 1372 branches accumulated; the shell-test tier collected by nothing, leaving ten tracked tests inert including the gate's own regression suite; `meteorite/run.sh` never run, so Hard Floor 5 was asserted rather than measured; and on 2026-08-04 `hygiene/check-retained-branches.ts` — written to close an evidence gap — landed correct and inert, its only caller a bootstrap stage parked without ACCEPT. Each was individually fixed. The **class** has never been attacked: nothing in the repository can answer "which tracked mechanisms are invoked by nothing?" The cost is not the writing, it is that a correct mechanism reads as protection while providing none, so the gap is invisible precisely while it matters. | a check enumerates tracked mechanisms (units, timers, crons, checkers, test tiers) and fails when one has no reachable executor; each known instance is either wired or carries a named, tracked exemption; the check itself runs somewhere it cannot silently lapse | **REOPENED 2026-08-04 by the Fable global review — was `done`, landed `1e4e0f2`.** The checker decides reachability with `text.includes(needle)` over tracked sources (`tools/check-mechanism-reachability.ts:61-74`) and so **accepts a code comment as an executor**, proven by mutation: rename the only file naming `check-shared-stash.sh` and it goes red; append `# see also check-shared-stash.sh` to an unrelated script and it goes green again. The defect class this row exists to detect, reproduced inside the detector. Consequences measured, not argued: of five required checkers only `checker:retained-branches` has a real production executor (`gate/land.sh:416`); `checker:shared-stash` and `checker:github-ref-protection` have none, and `runner:meteorite`'s needle `run.sh` is satisfied by eight tracked files including `docker/whisper-proof-run.sh` — so the Hard Floor 5 proof reads as reachable on the strength of an unrelated Whisper script. The claim that it encodes **installed is not armed** is false: the checker never reads host state (one `spawnSync`, for `git ls-files`), so the 2026-08-01 incident it was built from would report `clean` today. Worse than the gaps themselves: a green checker retires the question, and a wrong answer that closes an open question costs more than no answer. |
| V3-0.27 | **New 2026-08-04. Retained evidence can exist only on this host, and nothing notices.** Park records name a branch as "retained, do not reap", and `instance/hygiene-protected-branches.txt` stops the reaper deleting it — but **neither checks the branch is actually on origin**. Found by auditing: `ag-s5-5-r3`, V3-2.4's parked evidence, was protected locally and **absent from origin** until pushed manually. It was missed because its park record lives in the workboard row rather than `instance/parked/`, so a sweep over `instance/parked/*.md` did not see it. A destroyed host would have taken three review rounds of work with it — which is precisely the Hard Floor 5 claim the meteorite exists to defend, failing quietly in the evidence layer rather than the code layer. | a check fails when any protected or park-referenced branch is absent from origin, and it runs somewhere that cannot silently lapse | **done** — landed `6125296`. Every landing now runs `hygiene/check-retained-branches.ts` unconditionally (no changed-path predicate, and the `BUN_BIN` caller override is refused), verifying that each branch the repository promises to retain exists on origin. Three rounds: round 1 was correct and **inert** — its only caller was `install_hygiene_cron`, a bootstrap stage parked without ACCEPT, and `crontab -l` returned `no crontab for root`; round 2 wired it to the gate but made **landed** park records permanent obligations, so a legitimate cleanup of `ag-s6-13` would have blocked every landing; round 3 drew the disposition and locked both directions — a landed row's branch is history, a still-parked row's branch is still required and its removal reports `absent-from-remote`. Rollback now asserts ref position **and** tree cleanliness. The gap it closes was real: `ag-s5-5-r3`, three review rounds of V3-2.4's parked work, was protected from the reaper and had never been pushed — found only by a manual audit. |
| V3-0.26 | **New 2026-08-04. The no-progress detector cannot tell "stuck" from "blocked by something else".** HR-1780 asks that N consecutive rounds without a landed SHA park an item automatically, and the counter landed at `7c428e0` does exactly that. On 2026-08-04 it parked **V3-1.9** with `round=3 no_progress=3 parked=no-progress` — a row carrying **three independent ACCEPTs**. It had not landed because of V3-0.25: each unrelated landing made its report count stale and the gate correctly refused stale evidence. So the measured signal was real and its interpretation was wrong: the rounds produced work, the work was blocked elsewhere. The rule is right — an item can burn rounds while looking busy, which is why the operator asked for it — but it currently counts *rounds without a landing* and infers *no progress*, and those differ whenever the blocker is external. Note the counter deliberately has no lane- or orchestrator-callable reset (`instructions/review-policy.md:77`), which is the security property working as designed; clearing this park required the Human. | a round refused by a mechanism unrelated to the item under review does not count toward no-progress, or the park record names the external blocker so the distinction is visible; proven by a fixture where an unrelated landing invalidates a report and the item is not parked |
| V3-0.25 | **New 2026-08-04. Every landing invalidates the reports of every other branch in flight.** `gate/land.sh` compares the report's `verify-count` against the post-merge suite result and aborts on mismatch — correctly, since a stale count is not evidence about the merged tree. But any landing that adds tests changes that number for **every** branch already reviewed and waiting: `ag-s8-1-r3` was ACCEPTed with `524/0` and refused at landing with `LAND verify-count mismatch report=524/0 actual=536/0`, because the quota row (`472df6b`) landed in between. The remedy today is a rebase, a re-run, a fresh report **and a fresh review**, because the rebase moves the tip and the ACCEPT names the old SHA. So the cost of landing N reviewed branches is quadratic in N, and it grows with fleet size — at a floor of 10 the queue can starve. The check itself is right; what is missing is a way for a report to remain valid evidence across an unrelated landing (a delta-based assertion, a re-verify step that refreshes the count without discarding the review, or an explicit re-review only when the merge touches the branch). | landing a second reviewed branch after an unrelated landing does not require a new review round; proven by landing two independently reviewed branches back to back | **done** — landed `2937592` `[CODER] make reviewed count measurement gate-owned`, with `277ad31` before it; the counter records it landed at that SHA. This is the defect that invalidated V3-1.9s report count each time an unrelated row landed, and so caused its no-progress park. |
| V3-0.20 | **New 2026-08-03. Test fixtures create refs in the shared repository and poison other lanes.** An independent reviewer running the full suite in a worktree hit `orchestrator/fleet/launch-lane.test.sh` failing on the **pre-existing** shared ref `ag-fleet-launch-proof` — a leftover from another lane's fixture run, in the repository every lane shares. It correctly refused to delete a foreign ref and reported the interference instead. Same shape as the `refs/stash` collision (V3-0.7) and the orchestrator reset that killed a landing (`instance/incidents/2026-08-03-orchestrator-reset-during-landing.md`): **two writers on one piece of git state, where only one knows a transaction is in progress.** Third instance in one day, and each looked like an unrelated new bug. At the fleet floor of 10 this makes suite results depend on which lanes ran before. | fixture refs are created in a disposable repository, or namespaced and swept like `refs/meteorite-candidates/*`; a test proves a concurrent fixture run cannot alter another lane's result | **done** — landed `cd0b1c3` (tip `5702f99`, ancestor of `origin/main`; the durable counter records the same landing). Fixture refs are created in a disposable repository, so a concurrent fixture run can no longer alter another lane's result — the defect that was corrupting every lane's suite evidence. **Recorded honestly:** this row still read `in flight` six hours after it landed, and the Fable global review caught it as the third live instance of V3-0.30 — while the lane to fix V3-0.30 was itself in flight, and while the dispatch brief for that review named the landing. The board is not evidence; ancestry and the counter are. |
| V3-0.19 | **New 2026-08-03. Correct Tier-A lanes produce unlandable reports.** `instructions/lane-lifecycle.md:24` gives a lane two honest endings — pass (exit 0) or `NO-GO` (exit 3, "a legitimate parked row"). It names no state for *work finished and verified, independent review outstanding*, which is the **normal** ending for every Tier A row. So lanes correctly write `result: NO-GO`; the completion guard correctly refuses (`PASS result NO-GO (no-go-declared)` → `GUARD verdict=no-go` → `verdict=aborted`); and when the ACCEPT later arrives the report is frozen at NO-GO and can never land. Observed across all of sprint 04: seven Tier-A lanes, ACCEPTs arriving afterwards, zero landable reports. `gate/completion-guard.ts` already accepts the right shape — `result: clean` with `remaining: Tier-A review pending` and no `review:` field, the case V3-0.13 locked deliberately — so the mechanism is correct and only the instruction is missing. The orchestrator's own lane briefs offered `<clean\|NO-GO\|blocker>` without saying which applies to a pending review, which reinforced the wrong choice. | `instructions/lane-lifecycle.md` names the review-pending ending explicitly; a lane following it produces a report the guard accepts before review and that lands unchanged once an ACCEPT artifact exists |
| V3-0.18 | **New 2026-08-03. The landing gate can report a SHA it never pushed.** `gate/land.sh`'s push step treats git's `Everything up-to-date` as success, and the gate then emits `verdict=landed-reap-failed sha=b857786aa246c9d463e95c79085d587445089b93` for a commit that is on no remote at all. Reproduced live, not hypothesised: while lane `s5-11`'s landing was mid-flight in `land-main`, the orchestrator ran `git reset --hard origin/main` in that same clone (its own error, see below), discarding the merge; the gate's later push therefore had nothing to send, said `pass`, and reported a landed SHA. Every other step had verified honestly — freshness, completion-guard, review, secret-scan, branch-tip, payload-guard, baseline and post-merge verify all passed on real work. Only the final claim was false, which is the worst place for it: the reported SHA is the entire report contract, and "no SHA means not done" is worthless if a SHA can be reported for nothing. The gate must confirm the remote ref equals the SHA it is about to claim, and `Everything up-to-date` when a merge was just made is a **failure**, not a no-op. | after a landing, `git ls-remote origin refs/heads/<target>` equals the claimed SHA, or the verdict is not `landed`; a fixture where the push is a no-op makes the gate report failure | **done** — landed `4881b61` `[CODER] verify pushed landing SHA against remote`. Verified on `origin/main` 2026-08-04: after the push, `gate/land.sh` reads `git ls-remote --refs origin refs/heads/<target>` and fails with `LAND push remote-mismatch` unless it equals the merge SHA, then rolls back. `Everything up-to-date` can no longer be reported as a landing. |
| V3-0.17 | **New 2026-08-03.** A lane can finish without writing a terminal report and nothing notices. `gate/lane-exit.sh` exists and `core/mission-cli.ts lane complete` really invokes it (V3-0.5), but `orchestrator/fleet/launch-lane.sh` — the mechanism that starts **every real lane** — contains no reference to the report or the guard, and nothing calls `lane complete` on a lane's behalf. Observed twice the same evening: lanes `s5-8` and `s5-9` both ended with their report only in their log, so a Tier-A reviewer had no evidence to inspect and blocked on its absence. This is V3-0.5's own "remains, next row (a)" — real AI coder-lane dispatch has no code-mediated choke point — now with live instances rather than an argument. | a lane that exits without a report at its declared path is recorded as failed, not terminal; proven by dispatching one through the real launcher | **done** — landed `49ce421`. `gate/lane-exit.sh` now runs inside the systemd unit wrapper (`orchestrator/fleet/launch-lane.sh`), with `PIPESTATUS` capture and `LANE_REPORT_PATH` injected, so the agent does not choose whether the guard runs. Four states proven by the reviewer's own execution: silent → `state:failed reason:report-invalid`; valid → `state:terminal reason:report-valid`; report failing the guard → `state:failed reason:report-invalid`; crash → `state:failed reason:payload-exit exit:17` — a crash and a silence stay distinguishable. Two rounds: round 1 earned an ACCEPT and then failed its own landing with `LAND step=preflight status=fail detail=caller-bun-override-refused`, the nested-gate trap — its fixture called a gate script while `BUN_BIN` was set, so it passed by hand and failed under the gate, and the reviewer had run it by hand. Round 2 cleared the caller environment without weakening `land_resolve_bun`'s refusal, and added a lock that runs the fixture **with `BUN_BIN` set**. No launcher assertion was removed, verified line by line against the list in `instance/parked/V3-1.9-nonroot-lanes-2026-08-03.md`. Three lanes the same evening had ended with their report only in a log; that is now a recorded failure. |
| V3-0.16 | **New 2026-08-03.** Two live report contracts. `orchestrator/dispatcher.ts`'s `validTerminal()` runs its own bespoke fencing rules while `gate/completion-guard.ts` runs the real one; V3-0.5 hardened the parser but deliberately left the two unreconciled, because reconciling them needs the branch-field schema change V3-0.2 flagged as bigger than one row. Two contracts for the same artifact means one of them is wrong and nobody knows which — and the dispatcher's is exercised only by synthetic fixtures, so it has zero real traffic to catch it. See "Two live report contracts" below. | one parser, one contract; the dispatcher's synthetic-fixture path and the guard's real path accept and reject the same set of reports, proven by a shared table run against both | **done** — landed `39a7698` `[CODER] complete report contract parity cases`, verified on `origin/main` 2026-08-04: `gate/report-contract.ts` invokes the authoritative `gate/completion-guard.ts` and `orchestrator/dispatcher.ts:4,30` imports `completionReportVerdict` instead of running its own rules; `gate/report-contract-parity.test.ts` is the shared table. **This row stayed open on the workboard after it landed and a lane was dispatched onto it by mistake** — the earlier REJECT verdicts in the lane logs were superseded by a later ACCEPT, and the counter had recorded `landed=39a76988` all along. Filed as V3-0.30. |
| V3-0.15 | **New 2026-08-03.** `instructions/orchestrator-cold-start.md` instructs the orchestrator to run `mission transition` and `lane transition` CLI actions that exist nowhere in `core/mission-cli.ts`. Found during V3-0.5's investigation, flagged in the doc, never fixed. An instruction naming a command that does not exist teaches a cold-starting orchestrator to improvise at exactly the moment it has no context — and no test catches it, because nothing checks that documented commands are real. | a test extracts every `mission-cli` invocation from `instructions/` and asserts each names a dispatchable action; it is red against a fabricated action name | **done** — landed `c8e52f6` (`ag-s10-2-r3`, work `69adc55`), round 3 ACCEPTed. Both open findings closed by execution: the documented landing command is now runnable as written, and `mission complete` names its refusal reason. The reviewer re-ran cold start independently rather than trusting the lane's transcript, and confirmed the round-3 rebase dropped no lock. Two coverage findings left open by the reviewer's own judgement and carried to V3-0.41: the new lock only sees code blocks tagged `sh`/`bash`/`shell`/`console`, and a trailing backslash can desynchronise the fence tracker. Neither is a false green. |
| V3-1.9 | **New 2026-08-03, operator decision `opus_lane_execution_2026_08_03=nonroot_lanes`.** Every lane runs as **root**, and the Claude CLI refuses `--dangerously-skip-permissions` as root, so v3 cannot run Claude-provider lanes at all — the fleet is single-provider by constraint, which also blocks HR-1734/V3-3.2. Beyond provider choice: agents are told to bypass approvals, execute code they wrote, and are confined by nothing. Sub-rows named by the implementing lane: **1.9a** provision the `bpa-lane` account, its credentials and the `/srv` paths; **1.9b** live Claude and Codex proofs; **1.9c** privilege-boundary review. | a lane launched by the mechanism runs with a non-zero uid, a Claude-provider lane launches, and the Codex path still works | **PARKED 2026-08-03 at the HR-1726 cap** — three rounds, no ACCEPT, and the remaining blocker is **not code**: the row cannot finish until a service account exists and its provider credentials are minted by interactive login, which is operator-only (CLAUDE.md rule 14). Round 3 (`5fd4b0c`, branch `ag-s5-1`, retained and protected) adopted the correct single-service-user model and was REJECTed for **deleting regression locks** — atomic reservation, dangling-symlink refusal, failed-launch name reuse, positional command elements, private `TMPDIR` and the mask-stream path — so its green suite is a false green for launcher defects earlier rounds fixed. Settled and not to be re-litigated: the design (`instance/v3-nonroot-lane-model-2026-08-03.md`); inter-lane confinement is out of scope by evidence and disclaimed in `instructions/single-service-user.md:13-19`; every artifact is created after `exec setpriv`; missing user/linger/credentials/bad mode/failed setpriv all terminate rather than falling back to root; and `IPAddressDeny=localhost` is **honoured, not merely serialized**, on this host's systemd 255 root user manager (plain unit `exit=0`, filtered unit `exit=28`). Evidence and the lock the recut must satisfy: `instance/parked/V3-1.9-nonroot-lanes-2026-08-03.md`. Awaiting the operator on provisioning. Earlier rounds: round 1 (`56b1ba2`) REJECTed: the lane uid cannot write `.git/worktrees/<lane>/index.lock` so a coder lane cannot commit at all while the launcher still reports `launched`; all lanes share one `bpa-lane` identity and one lanes root, so inter-lane confinement does not exist; and the test mock never executes as the requested uid, which is what concealed the first defect. Round 2 (`ba597d4`, retained, not landed) reported `NO-GO` and fixed the git writes within the root-creates-then-chowns design. That design is now known to be wrong: the v1 orchestrator, asked over the file channel, answered that its line **never runs anything as root** — one lingering non-root service user owns the orchestrator, the daemon, the repository, the worktrees and the provider credentials, so the ownership handover, the `HOME` override and the `safe.directory` narrowing all cease to exist rather than being solved. It also stated plainly that the working line has **no** inter-lane confinement, which settles round 2's second blocker by evidence: uid-per-lane is a later hardening, not a precondition. Quoted in full with its limits at `instance/v3-nonroot-lane-model-2026-08-03.md`. |
| V3-1.8 | The two tests that fail in a clean container: `orchestrator/singleton-failclosed.test.sh` and `gate/land-rollback.test.sh`. | Both tests pass in a clean container, or carry an explicit named exclusion with the capability they need; both still pass on this host and retain red-before regression evidence. | **done** — clean-container assumptions are now measured explicitly: Docker does not expose the namespace's own flock through `/proc/locks`, its overlay filesystem rejects `chattr +i`, and its security profile rejects the PID/mount namespace fixture. Those cases emit named capability exclusions; all measurable rollback fixtures pass. Both complete tests pass on the host and both go red against a deliberately broken subject. |

## Phase 2 — supervision that does not depend on the Human noticing

The 2026-08-03 investigation found the watchdog had not run since 2026-08-01 12:35:57Z,
`orch-runtime-watchdog.timer` installed but never armed, and
`notify_human_below` declared in `params.yaml` and read nowhere.

| id | row | acceptance |
|---|---|---|
| V3-2.1 | Unattended orchestrator restart, armed by the bootstrap path, not by hand. | kill the orchestrator in a container; it is back within the configured interval |
| V3-2.2 | Human escalation when running lanes fall below `fleet.notify_human_below`. Make the knob live or delete it — a config promising escalation and delivering nothing is worse than none. | seed 0 lanes in the container; a Human-addressed message is emitted | **done** — landed `d01cd1c`. Two rounds. Round 1 wired the threshold to the daemon-owned outbox but could not prove anything reached the Human: its probe timed out after eight seconds with `methods=` empty and the local listener saw no request at all. It was also rejected for a **false green** in the counter lock — `orchestrator/watchdog.test.sh` seeded lanes by direct SQL, bypassing `mission-cli lane claim` and the fleet launcher, so the count could pass while the real dispatch-to-status seam was disconnected (the exact defect V3-0.6 exists for). Round 2 closed both, verified by the reviewer executing them: `orchestrator/watchdog-transport-boundary.test.ts:16-115` starts the real `daemon/server.ts`, places the alert in the outbox and observes `/sendMessage` at a local fixture; the counter now reads live leases created through dispatch ownership. Note what this does **not** prove: the real Telegram transport is untouched by design, and the fleet sat at 0 lanes for part of this evening with nothing escalating — that was the row unlanded, and it is the reason the row existed. |
| V3-2.3 | Deployment-drift detection that is not itself subject to the drift it detects. `bpa-deploy-drift-guard` was among the 8 units missing from the host, which is why the gap went unreported for days. | remove a deployed unit in the container; the guard reports it and exits non-zero |
| V3-2.4 | Orchestrator model pin asserted at startup; a mismatch between the pin and the live session is a fail-closed error. HR-1680: the orchestrator may never change its own model, and v3 must make it structurally impossible. | start with a mismatched pin; startup refuses | **PARKED 2026-08-03 at the HR-1726 cap.** Three rounds; round 3 (`ag-s5-5-r3`, retained) earned an independent **ACCEPT** and reported `result: clean`, and the landing still fails: `post-merge-verify` returns 1 failure, the host-dependent test filed as V3-2.8. Closed and verified across the three rounds: the checker and pin path are no longer selectable from the orchestrator-writable environment; `/model` no longer reports a success the next launch silently refuses; every degenerate input (missing, empty, mode-000, malformed, mismatch) refuses before startup side effects; removing `assert_model_pin || return $?` makes the suite fail, so the executor property is locked; and a well-formed secret-shaped request is no longer echoed verbatim — round 2 had locked that with a semicolon-bearing value, which is *malformed* and therefore exercised the masking path that already worked. **The honest scope, which must not be softened:** this stops accidental change and runtime drift, not determined self-modification, because every lane on this host runs as root and can edit the checker and the pin it is constrained by. HR-1680's literal "structurally impossible" is unreachable until the privilege boundary exists — V3-1.9, now in progress under HR-1962 but not landed. |
| V3-2.7 | **New 2026-08-03.** The meteorite dirties the repository it is proving. `meteorite/run.sh:17` defaults its report to `$repo_root/reports/meteorite-latest.md` and `reports/` is not ignored, so any run leaves an untracked file in the working tree — and `gate/land.sh` correctly refuses to land into a dirty tree (`LAND step=working-tree status=fail`). Hit directly by the orchestrator: after running the rebuild proof, the next landing aborted until the artifact was moved out by hand. Two mechanisms that must both work on the same checkout are coupled through the working tree, and the failure names neither. | running the meteorite leaves the tree clean by `git status --porcelain`, and a landing immediately after a meteorite run in the same checkout succeeds without manual intervention | **done** — closed by `7cca908`: the report defaults to `$XDG_STATE_HOME/bpa-dev-infrastructure/evidence/meteorite-latest.md` (`meteorite/run.sh:18`), outside the repository. Verified by execution 2026-08-04: two runs with the default report path left `git status --short` empty, and no mechanism writes a repository `reports/` path. |
| V3-2.6 | **New 2026-08-03.** `meteorite/run.sh` fails closed when its two required inputs (`METEORITE_DONOR_SHA`, `METEORITE_DONOR_REF`) are unset, but the blocker it emits is `test-prerequisites command failed` — which names neither variable nor the supported entry point `meteorite/prove-candidate.sh`. Found by the orchestrator running it directly. Failing closed with an unactionable message is correct behaviour delivered uselessly: the next operator debugs the wrong stage. | with each required input unset in turn, the emitted blocker names that input; a test locks the message to the input list so a new required input cannot be added silently | **done** — closed by `8f96908`. Verified by execution 2026-08-04, each input unset in turn: `NO-GO input-validation: required input METEORITE_DONOR_SHA is unset or empty; use meteorite/prove-candidate.sh --ref <40-character-commit-sha>`, and the same naming `METEORITE_DONOR_REF`. The input list is locked by `meteorite/meteorite.test.ts:78-79`, which iterates it. |
| V3-2.5 | The watchdog could not consume `mission-cli status`: missions were raw snake_case rows, lanes omitted `updatedAt`, and `leases` was absent, so mission pressure crashed before emitting a nudge. | `bash orchestrator/watchdog.test.sh` emits the expected genuine-status idle nudge; a missing contract field reports explicit NO-GO; the nine-test shell inventory runs with no exclusions | **done** — landed `7149da8`. Fixed producer-side: `reconstruct()` returns typed records with a real `leases` collection, and the watchdog validates arrays/strings/timestamps before arithmetic, emitting `WATCHDOG NO-GO reason=mission-pressure-status-contract-invalid` instead of the old silent `continue`. Round 1 was rejected for two semantic defects: a terminal lane retaining stale lease columns still counted as an active lease, and every lane of a mission counted as `open_lanes`, so the watchdog would nudge completed work forever. Round 1's report also claimed an independent ACCEPT that never happened; the landing gate refused it (`review-required missing-artifact`), which is what V3-0.13 exists to make impossible in the report itself. |

## Phase 3 — requirements that were agreed and silently dropped

From the requirements-coverage audit. These are not new features; each was ruled by the
Human and then lost when the fork copied a file tree instead of a ledger.

| id | row | source |
|---|---|---|
| V3-3.1 | `/model` switches the live session, not only the next relaunch. | HR-1349 |
| V3-3.2 | Spark routable as the cheap tier for mechanical lanes; quota is measured today and cannot be spent. | HR-1734, restating 1562 |
| V3-3.3 | Weekly per-provider model discovery with fit metadata, pluggable providers, fail-closed to the last committed catalog. | HR-1739 |
| V3-3.4 | Review round cap enforced by a counter and the gate, not by prose. The current rule carries a PROSE ONLY banner because nothing counts rounds. **Plus a no-progress detector** (operator ask, Telegram 1780): the cap counts attempts, which is not the same as detecting a stuck item. If N consecutive rounds on one item produce no landed SHA, park it automatically — an item can burn rounds while looking busy, which is exactly what the `authority` epic did across r6–r23. | HR-1726, HR-1780 | **done** — landed `7c428e0`. Parked once at its own cap, then unparked on the operator decision to finish honest counting rather than narrow the rule; five attempts in total. Attempts persist to origin as **mirrored refs**, so a fresh clone reconstructs an exhausted cap and parks — the earlier design wrote state only after a successful merge, so an attempt that counted and then failed vanished and a new clone repeated it. Tampering fails closed in both directions: deleting one genuine attempt ref, and forging a sequential ref at the lane commit, each produce `LAND review-rounds attempt-mirror-mismatch`. The DB is a cache only — deleting it changes nothing but speed, per the operator ruling that git history is the source of truth. Round 4 was rejected for **deleting** the `LAND step=push status=fail` and `main reset to origin/main` assertions while adding its own; round 5 restored them alongside. Honest limit, stated in `instructions/review-policy.md` rather than implied away: every lane runs as root, so this is tamper-**detecting**, not tamper-proof, until the privilege boundary lands. |
| V3-3.5 | Decision-request options explained in the message body; a button label is ~3 legible words. | HR-1752 |
| V3-3.6 | Vendor quota display, so a quota cliff is visible before it is hit. | ML-6 | **done — landed `472df6b`.** Quota is shown per provider in `/status`, sourced from real Codex rate-limit events, with the age of the observation. Three rounds. Round 1 claimed a regression lock that did not exist, which is what let three defects ship: `-7%` and `140%` rendered as confident percentages, and a future timestamp rendered as `вік 0хв` — freshly observed. Round 2 closed those but bounded a **fragment**: `formatVendorQuota` was ≤600 while `buildHumanStatus` appended a suffix and reached 619. Round 3 moved the bound onto the message the operator actually receives and locked it there. Claude is honestly reported unknown — its CLI does not expose quota locally — which was the point of the row: a confidently wrong figure is worse than none, because it is trusted precisely when it matters. | **done** — landed `d956e5d` `[CODER] bound final operator quota status line`, after `0c29d94`, `42c46a9` and `53ea894`; the counter records it landed at that SHA. |
| V3-3.7 | Self-hosted, user-teachable OCR. | HR-1418 |
| V3-1.11 | **New 2026-08-04, HR-2120. The installation does not fit itself to the machine it lands on.** The meteorite proves the repository can rebuild a host; nothing makes the rebuilt host behave sensibly on *different* hardware. Every limit here is a constant derived from nothing — fleet floor, deadlines, concurrency — and each would travel unchanged to a machine with different cores and be equally wrong there. His design: bootstrap installs, the human authenticates a provider CLI (already unavoidable — credentials are interactive), and once a model is reachable it measures the machine, proposes the behaviour with its reasoning, agrees it with the person doing the install, and writes the agreed values into tracked instance config. Same principle as HR-2088 applied to hardware: the system discovers a moving external fact instead of being told it once. Measured here for contrast: 251 GB RAM with 4 GB in use, 12 cores at load 16 — over-provisioned for memory, under-provisioned for cores, so fleet size should derive from cores. The one high-return use of the spare memory is converting it into the scarce resource: lane worktrees and test scratch on tmpfs, removing disk latency from the step that is currently the bottleneck. Verbatim: `instance/decisions/HR-2120.md`. | on a machine with different cores/memory the installation proposes different limits and says why; the human can accept or override, and what is agreed lands in tracked instance config so the same machine rebuilds identically while a different machine re-negotiates; no limit in the repository is a constant that survives a hardware change unexamined |
| V3-1.10 | **New 2026-08-04, HR-2109.** Split the channel into two bots at two privilege levels: an **administrator** running as root in its own operator-only chat, always on (machine up implies admin up), which takes commands only from the operator and from the orchestrator only *requests* — and the orchestrator, unprivileged, which may read the administrator record of an authorization and never write it. His own argument is the load-bearing one: the orchestrator has something to gain from forging an approval (less work for it), the administrator has nothing, so the construction stops depending on the orchestrator being honest. This supplies the operator door V3-0.29 was built for, without the SSH key that failed his real constraint — he decides from a phone, from anywhere. It also gives him a recovery path to a wedged orchestrator without touching the machine, which today does not exist. The load-bearing condition is that the orchestrator cannot WRITE the authorization record — writing is a privilege question, not a messaging one. The orchestrator argued that a readable admin token would be equally fatal; the operator rejected that and was right (Telegram 2111): holding the token buys deception and interception, not authority. Token stays root-owned as hygiene, not as the wall. The condition that does stand: the admin accepts requests from the orchestrator, never commands. Requires V3-1.9 — without non-root lanes, root-owned means nothing. Verbatim: `instance/decisions/HR-2109.md`. | the operator authorizes from Telegram and the record is written by a process the orchestrator cannot write to, proven by executing a forgery attempt as the orchestrator user and having it fail; the administrator survives an orchestrator restart and can recover a wedged orchestrator without SSH; the admin token is unreadable by the orchestrator user, proven by execution |
| V3-3.8 | **New 2026-08-04, HR-2088.** `model` with no argument must answer instead of failing: the model the orchestrator is running right now, plus the models available **from the current provider only**, each copy-pasteable so that pasting one works. He gave the reason himself — model identifiers change, and the infrastructure must not depend on a human keeping a list in a guide. Verbatim requirement: `instance/decisions/HR-2088.md`. Note HR-1680 still binds: this row makes `model` inform, never switch. | bare `model` from Telegram returns the active model and the current provider's list; a listed identifier works when pasted back; when the provider cannot be asked, the reply says so and names the last known catalog and its age rather than inventing one | **in flight** as `ag-s10-9`. |

## Phase 4 — cutover

Only after Phases 0–2 hold.

| id | row | acceptance |
|---|---|---|
| V3-3.10 | **New 2026-08-04, operator ruling HR-2188. Consumption is displayed but never recorded, so every argument about cost is made from impressions.** V3-3.6 shows quota now; nothing persists it. The operator asks for per-provider, per-hour consumption in a database that outlives a session, and a page with a graph he can click through — quota level if obtainable, input and output token counts as the fallback he names himself. Two design constraints that must be settled before code: providers do not expose the same numbers, and a provider with no quota signal must be recorded as **unmeasured, never as zero** — Hard Floor 7, and a graph is exactly where a zero silently reads as "we used nothing". Beyond his stated reason it earns its place twice: it makes the HR-2166 escalation ladder decidable, since that ladder trades cheap rounds against expensive ones and nobody can currently say what a round costs on each tier; and it gives V3-3.9 its detector, because a rate is what tells you exhaustion is coming, and exhaustion mid-flight is a work-loss event. | per-provider hourly consumption is persisted and survives a restart; a provider that exposes no quota signal is recorded as unmeasured and renders as such rather than as zero; the page renders from the stored data and not from a live call; proven by restarting the daemon and finding the history intact, and by feeding a provider that reports nothing | **open — acceptance extended 2026-08-04 by HR-2377.** He now names the exact shape: count and persist **input and output token counts, per model and per role**, in the database, so graphs can be drawn later. That is a sharper requirement than "per-provider consumption" and a more buildable one — the orchestrator established the same evening that it *cannot* read Claude quota at all (`daemon/vendor-quota.ts` returns Claude as `state: 'unknown'`; only Codex has a JSONL), so percentages depend on him sending a screenshot while **token counts are observable at the point of use**. His instruction replaces an unmeasurable metric with a measurable one. Per model *and* per role is load-bearing rather than decorative: every live cost argument on this board is about tiers — whether HR-2166's escalated round is worth it, whether simple work belongs on a cheaper model (messages 1562, 1841, 2092, 2186), what a lane-hour costs (HR-1494) — and none of them can be settled by a single total. The unmeasured-never-zero rule still governs. The new state it creates must join the V3-2.9 enumeration on the day it exists, and is a candidate for the HR-2171 backup set. |
| V3-4.1 | v3's daemon proven to hold the Telegram channel across a restart. | restart in the container; a round-trip message survives | **in review** — `d88b1af` (`ag-s10-5`) claims the hard case: an update queued while the daemon is down, SIGKILL inside the send/response ambiguity window, exactly one reply after reconciliation, in a container with `--network none` and a fixture token, with a red-before. Under independent review, whose first task is to establish whether the proof exercises the production daemon or the 30-line fixture daemon the change adds. Round 1 REJECTed: **the proof restarts a purpose-built 30-line fixture daemon, not the production daemon**, and the two-consecutive-ambiguity exactly-once case is unmeasured. Everything else came back closed — container red-before reproduces, the stub rejects malformed requests, waits are condition-based and bounded, run-container credential and network containment holds, the runner has no path to the live host daemon, and the reviewer got a real suite triple. |
| V3-4.2 | Clean-machine rehearsal from a bare container using only this repo. | `meteorite/run.sh` green from a fresh clone |
| V3-4.3 | Host cutover — the operator's "почищу сервак і з нуля почнемо". | his explicit go, after 4.1 and 4.2 |

---

## Two live report contracts — read before touching lane completion again

V3-0.2 set out to wire `gate/completion-guard.ts` at lane-exit time (done: see
`gate/lane-exit.sh`, `instructions/lane-lifecycle.md`,
`instructions/orchestrator-cold-start.md` §2.5). Investigating the wiring found
a second, pre-existing, un-related terminal-report contract that the row as
briefed would have left silently untouched:

1. `gate/completion-guard.ts` — the CLAUDE.md contract: `commit:` / `verify:` /
   `result:` / `secret-scan:` / `remaining:`, git-branch-aware (checks
   `commit:` against a branch tip). Used by `gate/land.sh` at landing time, and
   now also by `gate/lane-exit.sh` at lane-exit time. **This is the
   authoritative contract for real, git-branch-centered coder-lane reports** —
   the path documented in `instructions/orchestrator-cold-start.md` and the one
   every dispatched coder lane is taught.
2. `orchestrator/dispatcher.ts:validTerminal()` — a separate, older, lower-level
   contract: `lane:` / `attempt:` / `commit:` / `result:` plus
   laneId/fencingToken/ownerToken authentication, checked inline (no
   subprocess, no completion-guard.ts). It has no branch concept at all —
   `LaneRecord` carries no branch field — so it cannot check tip-pinning. It
   guards `core/DurableStore`'s fenced dispatch/retry state machine
   (`dispatchOnce`/`reconcileRunning`), exercised today only by the synthetic
   workers under `tests/fixtures/*.ts` (`noop-worker.ts`, `gated-worker.ts`,
   `forged-worker.ts`, `never-exit-worker.ts`). It is NOT the path real AI
   coder lanes go through — `orchestrator/dispatch-lane.sh` says outright "the
   repo has no lane launcher yet" — so this contract has no live coder lane to
   diverge from today, but it is real, tested, merged code, and it disagrees
   with contract 1 in both field names and in whether tip-pinning is checked
   at all.

These were deliberately NOT reconciled in V3-0.2 — full reconciliation needs a
branch field on `LaneRecord`/`DurableStore` (a schema change touching
`core/schema.ts`, `core/mission-cli.ts`, and every dispatcher test fixture),
which is bigger than this row. `orchestrator/dispatcher.ts` carries a comment
at `validTerminal()` pointing back here. Remaining work, next row: either (a)
add branch-awareness to `LaneRecord` and make `validTerminal()` delegate to
`gate/completion-guard.ts` for report-shape and tip-pinning, keeping only the
attempt-fencing fields as dispatcher-specific, or (b) if `dispatcher.ts`'s
fenced primitive is retired before it is ever wired to real lanes, delete
`validTerminal()`'s bespoke contract instead of reconciling it. Do not treat
V3-0.2 as fully closing "one report contract" until one of those happens.

V3-0.5 update: the two contracts are still not the same shape (that still
needs the branch-field schema change above), but they no longer parse their
respective `label: value` lines two different ways. `gate/report-contract.ts`
extracts `gate/completion-guard.ts`'s anchored, single-match `lineValue()`
parser; `orchestrator/dispatcher.ts`'s `validTerminal()` now imports and uses
the same function instead of a `report.includes()` substring test (which a
regression test proves could be fooled by the right text appearing anywhere
in the report, not only on its own `commit:` line). Separately,
`core/mission-cli.ts`'s `lane complete` — the one real, git/filesystem-aware
caller of `DurableStore.completeLane()` (`orchestrator/dispatcher.ts` calls
it directly too, but only from the synthetic fenced-dispatch path) — now
shells out to `gate/lane-exit.sh` and refuses to record a lane as terminal
when the guard fails. See the V3-0.5 row above for what that does and does
not close.

## Nested gate invocations — a property to know before writing a `verify:`

`gate/land-lib.sh:19-23` (`land_resolve_bun`) refuses to run when `BUN_BIN` is already
set in the environment:

```sh
if [ -n "${BUN_BIN:-}" ]; then
  echo "LAND step=preflight status=fail detail=caller-bun-override-refused" >&2
  return 1
fi
```

That is a deliberate supply-chain control: a caller must not be able to choose which
`bun` binary the landing gate trusts. It resolves only from the fixed host baseline
`/usr/local/bin:/usr/bin:/bin` and canonicalises symlinks.

The consequence is easy to trip over. `gate/land.sh` exports `BUN_BIN` for its own
run, so **any test that itself invokes `gate/land.sh` will fail when used as a
landing's `verify:` command** — the nested gate correctly refuses. It passes standalone
and fails under the gate, which looks exactly like flakiness and is not.

This cost roughly an hour of misdiagnosis on 2026-08-03, including a workboard row
wrongly marked blocked. The fix in a `verify:` line is `env -u BUN_BIN`, which restores
the standalone condition without weakening the control — the nested gate still resolves
its own bun from the trusted path.

Note also that this host carries two bun installations: `/usr/local/bin/bun` 1.2.22
(the one the gate's trusted path finds) and `/root/.bun/bin/bun` 1.3.14 (the one on
the interactive `PATH`, which lanes test with). The gate is not wrong to pin the
baseline, but lanes verifying on a different version than the gate is a skew worth
closing in Phase 1.

## Standing check this plan creates

The fork copied files and lost rulings. `tools/check-decision-ledger-drift.sh` now
closes that for the decision ledger. The same shape is still open for mechanisms: a
scheduled inventory diff of executable mechanisms between this line and
`v2-deprecated`, failing until each absence is either ported or dispositioned with a
reason. Until that exists, Phase 1's list is a snapshot that can go stale the same way
the ledger did.
