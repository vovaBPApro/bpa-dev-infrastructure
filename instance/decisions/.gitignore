# Runtime capture artifact — raw inbound chat, never committed (secret-hygiene:
# inbox.jsonl carries verbatim Human messages that may contain sensitive words).
# HR-*.md and redacted triage.jsonl decision records ARE tracked.
inbox.jsonl
