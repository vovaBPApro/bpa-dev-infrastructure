---
id: hr-11582
layer: L1
status: binding
audience: orchestrator
tags: [instance, decision]
summary: The old daemon's /status often showed irrelevant info (e.g. "0 coders" while the orchestrator had 3 running); fix its truthfulness on the new layout, and consider a multi-model GPT consilium to review the instructions.
date: 2026-07-29
routes-to: status-truth-fix, gpt-consilium-instruction-review
state: routed
---

# HR-11582 — /status truthfulness + GPT-consilium instruction review

Verbatim (Vova, 2026-07-29, Telegram 11582, VovaBPApro, ts
2026-07-29T08:14:56Z):

> може додатково варто підняти консиліум на різнихх моделях гпт щоб щє він передивився інструкціі і все інше та скуазав що варто додати чи поміняти щоб воно і на сол моделі все максімально якісно працювало?команда /status в старому демоні погано працювала - часто показувала якусь нерелевантну інфо, наприклад часто було таке що показувало 0 кодерів, а орк казав що хз там 3

Effect — two separate threads:

1. `/status` truthfulness (this fix). The new daemon imported the old
   `/status` builders (`daemon/server.ts`: `activeAgentLines`,
   `buildOrchRuntimeStatus`) unchanged, and they were wrong on the new repo
   layout: the active coder-lane count came from a hard-coded
   `~/BPAprojects/agent-bill` worktree path (absent here) or from
   `orchestrator-state.json`'s `agents_active` field (also absent), so it
   printed "0 coders" while lanes were actually running. Fix: derive the lane
   count from real ground truth — `git worktree list` of the canonical infra
   repo filtered to `/home/bpa-shell/.cache/infra-lanes/*` worktrees on `ag-*`
   branches — and make every state-file-backed field degrade honestly
   (`n/a (no <file>)`), distinguishing "0 (verified)" from "unknown".
   routes-to: `status-truth-fix` (this lane; `daemon/status.ts` +
   `daemon/status.test.ts`, regression-locked with real fail-before/pass-after).

2. GPT consilium re-review of the instructions. The Human asks whether it is
   worth raising a multi-model GPT consilium to re-read the instructions and
   everything else and say what should be added or changed so the framework
   also runs at maximum quality on the SOL model. This is handled SEPARATELY
   from the code fix. routes-to: `gpt-consilium-instruction-review` — an
   orchestrator-scheduled consilium pass over the instruction set, not part of
   this coder lane.
