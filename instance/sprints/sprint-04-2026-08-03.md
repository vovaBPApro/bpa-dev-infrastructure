# Sprint 04 — 2026-08-03 evening

Opened at `1f81831`. Requested by the operator directly (Telegram 1944: «Давай щє
спрінт заплануємо і проженемо»).

## What changed since sprint 03 closed

Sprint 03 closed with Phase 0 essentially done and Hard Floor 5 unproven. It is now
proven: `meteorite/prove-candidate.sh` returned `result: clean` at `e17f629` from a
clean container with no file from this host. That unblocks Phase 2, which was
waiting on a rebuild proof to be worth anything.

The second change is external. The v1 orchestrator answered over the file channel
and its answer inverts V3-1.9's design — see
`instance/v3-nonroot-lane-model-2026-08-03.md`. Round 2 built the right thing under
the wrong model.

## Rows in this sprint

| lane | row | why now |
|---|---|---|
| s5-1 | V3-1.9 round 3 | last round under HR-1726. Rebuild on the single-service-user model the v1 line proved in production, instead of the root-hands-down-privilege model that has failed twice. |
| s5-2 | V3-1.1 stage 2 recut | parked at the round cap 2026-08-03. The park record names the exact lock the recut must satisfy, so a fresh lane starts with more than the last one had. |
| s5-3 | V3-2.2 | `fleet.notify_human_below` is declared and read nowhere. The fleet hit 0 running lanes this evening and nothing escalated — the row's own failure mode, live. |
| s5-4 | V3-2.3 | `bpa-deploy-drift-guard` was itself among the 8 units missing from the host. A drift detector subject to the drift it detects. |
| s5-5 | V3-2.4 | HR-1680 — the orchestrator may never change its own model. Prose today; must be structural. |
| s5-6 | V3-3.4 | the review round cap carries a PROSE ONLY banner. Three rows were parked at the cap today by hand-counting. |
| s5-7 | V3-2.6 | found by running the meteorite: it fails closed with a message that names neither missing input. |
| s5-8 | V3-0.15 | `instructions/` tells a cold-starting orchestrator to run CLI actions that do not exist. |

## Sequencing

s5-1 and s5-2 both touch `bootstrap/`. s5-1 is confined to a **new** file so the two
cannot conflict in `install.sh`; the wiring between them is a follow-up row, filed
by whichever lands second.

No lane touches `instance/workboard.md`. Row states are written by the orchestrator
in the landing commit — sprint 03 burned four review cycles on a workboard edit that
landed under three lanes in review.

## What is not in this sprint, and why

- **V3-1.7** (three-level hierarchy) — blocked on the operator restating the model
  in his own words. HR-1876 is `status: backlog` and stays there.
- **The cast** (`instance/cast.md`) — the operator asked to fix the full name list
  once he has finished answering; two mechanisms are still unnamed. His call, not a
  lane's.
- **V3-0.14 live application** — the GitHub ruleset is the operator's, by his own
  decision `github_branch_protection_2026_08_03=operator_ui`.
- **V3-4.3 host cutover** — his explicit go only, after 4.1 and 4.2.
