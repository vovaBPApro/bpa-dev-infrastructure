---
id: hr-11573
layer: L1
status: binding
audience: orchestrator
tags: [instance, decision]
summary: When Fable/Anthropic quota is exhausted and the orchestrator runs on a fallback (GPT) model, extra fallback-mode instructions apply.
date: 2026-07-29
routes-to: orchestrator-fallback
state: routed
---

# HR-11573 — Fallback-model orchestrator instructions

Verbatim (Vova, 2026-07-29, Telegram 11573, VovaBPApro, ts
2026-07-29T08:07:24Z):

> доречі щє треба подумати про те що інколи квота на фейбл і антропік
> закінчується, і тоді я піднімаю оркестратора на гпт, може нам для цього
> випадку треба якісь додаткові інструкціі? чи про якісь щє важливі нюанси
> подумати?

Effect: the fallback-model operating case is a real, recurring one — when Fable
/ Anthropic quota runs out, the Human raises the orchestrator on a non-Anthropic
(GPT) model. It needs its own binding instructions. routes-to:
`orchestrator-fallback` (`instructions/orchestrator-fallback.md`), covering
session load without a Claude hook, memory as vendor-local cache, review
independence under a shared vendor, deferred Fable-only finalization, and the
switchover handoff both directions.
