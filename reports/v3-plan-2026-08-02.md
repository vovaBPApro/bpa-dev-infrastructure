# Infrastructure v3 — executable synthesis for HR-1382

## Consumption check

- `orchestrator-playbook` `sha256:2c5d9a8dc6b3` — Orchestrator Playbook
- `orchestrator-fallback` `sha256:811f13bc3373` — Orchestrator Session Portability
- `autonomy-and-capacity` `sha256:18c43aaf7e14` — Autonomy and Capacity
- `landing-and-merge` `sha256:951d9781cffa` — Landing and Merge
- `roles` `sha256:cd4c40c4e640` — Roles
- `instruction-layers` `sha256:cd21f4ce0990` — Instruction Layers
- `reproducible-from-git` `sha256:822d9efe694b` — Reproducible From Git

Inputs consumed in full: `instance/decisions/HR-1382.md`,
`instance/decisions/HR-1374.md`, `instance/decisions/HR-1379.md`, mandatory
`/root/.cache/infra-lanes/data-hist/consilium-context-pack.md`, and all five
listed consilium/incident reports. NEW was examined at
`e695702faf5adbb53a8e0e9698b44e7c4643711d`; OLD donor at
`d0a99b8439f2731654e23b5e7759961f4602d0d3` (with the verdicts also inventorying
rescue `5c8206be024f49538696d6237021ce4e4a70b5ca`).

## Synthesis verdict

Build the defined hybrid on the already-created orphan `v3`: NEW supplies the
generic Bun/TypeScript control plane, instruction compiler, evidence/landing
gates, tracked installation and isolation; OLD supplies behavioral contracts and
fixtures for confirmed dispatch, durable queue/mailbox, hierarchical status and
restart continuity. Do not merge either tree, copy its history/runtime state, or
claim the OLD `1→3→9` concept is already production-proven.

Tonight is a **bootstrap proof, not cutover and not an unattended-night
admission**. In 2–4 hours it must prove that a clean clone builds a self-configuring
Docker stand which starts (1) durable recovery state/supervision, (2) a Telegram
channel with truthful status and an independent test transport, and (3) one
confirmed dispatcher/worker lifecycle with terminal evidence. Real Telegram
credentials are optional operator-supplied runtime inputs and never enter the
image or git. The stand must pass fully with a deterministic fake Bot API; a
separate opt-in live-channel probe may use injected secrets.

## Tonight's exact slice

### Copy from NEW into the empty `v3` root

Copy tracked content, preserving paths, only from this allowlist:

- Root/toolchain: `package.json`, `bun.lock`, `tsconfig.json`, `.gitignore`,
  `CLAUDE.md`, and the `AGENTS.md` symlink.
- Instruction/capture floor: `instructions/`, `instance/params.yaml`,
  `instance/tags.conf`, `instance/decisions/`, `tools/instructions/`.
- Durable substrate: `core/state.ts`, `core/state.test.ts`,
  `core/capabilities.ts`, `core/capabilities.test.ts`, `core/mission-cli.ts`,
  `core/mission-cli.test.ts`.
- Telegram/channel slice: `daemon/package.json`, `daemon/bun.lock`,
  `daemon/tsconfig.json`, `daemon/server.ts`, `daemon/control.ts`,
  `daemon/status.ts`, `daemon/history-logger.ts`, `daemon/inbox-mirror.ts`,
  `daemon/reliability.ts`, `daemon/relay.ts`, `daemon/model-registry.ts`,
  `daemon/notify-handler.ts`, `daemon/test-env.ts`, `daemon/secret-masker.ts`,
  and the matching `*.test.ts` files for those modules. This is a temporary
  adapter boundary for tonight; D2 splits the monolith before expansion.
- Recovery/provider slice: `orchestrator/launch.sh`, `orchestrator/lib.sh`,
  `orchestrator/proc-identity.sh`, `orchestrator/watchdog.sh`,
  `orchestrator/status.sh`, `orchestrator/runtime.env.example`,
  `orchestrator/orchestrator-liveness-pulse.sh`, plus
  `orchestrator/launch-handshake-bounded.test.sh`,
  `orchestrator/singleton-failclosed.test.sh`,
  `orchestrator/watchdog.test.sh`,
  `orchestrator/watchdog-supervision.test.sh`, and
  `orchestrator/watchdog-transport-boundary.test.ts`.
- Isolated dispatch primitive: `orchestrator/fleet/launch-lane.sh`,
  `orchestrator/fleet/launch-lane.test.sh`, `orchestrator/dispatch-lane.sh`.
- Evidence gate needed by the demo: `gate/land-lib.sh`, `gate/land.sh`,
  `gate/completion-guard.ts`, `gate/review-policy.conf`, and their directly
  named `*.test.*` fixtures.
- Install/drift basis: `bootstrap/install.sh`,
  `bootstrap/deploy-host-mechanism.sh`, `bootstrap/check-deployed-drift.sh`,
  `bootstrap/check-unit-drift.sh`, `bootstrap/env.template`,
  `bootstrap/telegram-transport-preflight.sh`,
  `bootstrap/telegram-transport-preflight.test.ts`, and
  `bootstrap/units/bpa-orchestrator.service.in`,
  `bootstrap/units/bpa-orchestrator-watchdog.service.in`,
  `bootstrap/units/bpa-orchestrator-watchdog.timer.in`,
  `bootstrap/units/bpa-telegram-daemon.service.in`.

Do **not** copy NEW `orchestrator/fleet/fleet-nudge*`, `as-run-wave*.sh`, runtime
files, installed units, reports, worktrees, databases, logs, `.env` files, or
the remainder of the 4318-line daemon surface merely because it exists.

### Copy from OLD donor into `v3/vendor/old-contracts/`

These are immutable reference fixtures/contracts for tonight, not executable
production imports. Copy each file under the same suffix beneath
`vendor/old-contracts/` and record donor SHA in `vendor/old-contracts/SOURCE.md`:

- `tools/orchestrator/dispatch-agent-spawn-contract.test.sh`
- `tools/orchestrator/dispatch-agent-lane-report.test.sh`
- `tools/orchestrator/dispatch-agent-no-commit-proof.test.sh`
- `tools/orchestrator/lane-survives-dispatcher-death.test.sh`
- `tools/orchestrator/mission-queue-dispatch.test.sh`
- `tools/claude-telegram-daemon/status-collector.test.ts`
- `tools/claude-telegram-daemon/orchestrator-runtime-status.test.ts`
- `packages/master-orchestrator/src/__tests__/mailbox-ipc.test.ts`
- `packages/master-orchestrator/src/__tests__/mailbox-replay.test.ts`
- `packages/master-orchestrator/src/__tests__/handoff-records.test.ts`
- `deploy/systemd/bpa-orchestrator.service`
- `docs/ops/orchestrator_durable_boot.md`

Read behavior from, but do not copy implementation wholesale tonight:
`tools/orchestrator/dispatch-agent.sh`,
`tools/orchestrator/mission-queue-dispatch.sh`,
`tools/orchestrator/orchestrator-loop.sh`,
`tools/claude-telegram-daemon/status-collector.ts`,
`tools/claude-telegram-daemon/orchestrator-runtime-status.ts`,
`packages/master-orchestrator/src/mailbox-ipc.ts`, and
`packages/master-orchestrator/src/handoff-records.ts`. No OLD product package,
pnpm workspace, credential, host constant, branch, history, or runtime state is
admitted.

### Seams to write fresh

- `docker/Dockerfile`: pinned Bun base; non-root runtime user; installs only
  declared packages; no host mounts except explicit test output; healthcheck.
- `docker/compose.meteorite.yml`: `fake-telegram`, `control-plane`, and
  `meteorite-runner`; named volumes created by Compose; no host systemd/tmux.
- `docker/fake-telegram.ts`: deterministic Bot API `getUpdates`/`sendMessage`,
  delivery ledger, forced-failure switch; no real token.
- `bootstrap/container-install.ts`: idempotently creates runtime dirs/database,
  renders container service config, validates permissions, applies no host-only
  mutation, and emits a machine-readable install manifest.
- `core/schema.ts` and `core/schema.test.ts`: tonight's minimum durable records:
  mission, manager, lane, parent/depth, generation, lease/deadline/retry budget,
  acceptance id, acknowledgement, semantic-progress timestamp/evidence path,
  terminal SHA/report/verdict, and outbox delivery state.
- `orchestrator/dispatcher.ts` and `orchestrator/dispatcher.test.ts`: claim one
  ready row transactionally, launch one isolated worker, require acknowledgement,
  persist terminal evidence, retry boundedly, and mark `NO-GO` loudly. It must
  never call tmux or treat process/channel heartbeat as progress.
- `orchestrator/supervisor.ts` and `orchestrator/supervisor.test.ts`: rebuild
  state after dispatcher death, fence expired ownership, and send independent
  escalation through the outbox. Tonight it need prove one worker, not 10–12.
- `daemon/adapters/telegram.ts`, `daemon/outbox.ts`, and tests: durable inbound
  before acknowledgement, idempotent outbound attempts, fake/live endpoint
  injection, truthful degraded state.
- `orchestrator/status.ts` and test: joins channel, provider, mission, manager,
  worker, lease, last semantic evidence and delivery state; unknown is never
  green.
- `tests/fixtures/noop-worker.ts`: deterministic worker that acknowledges,
  writes an artifact, terminal report and synthetic commit id, then exits.
- `tests/meteorite.test.ts`: black-box acceptance driver.
- `scripts/meteorite.sh`: the Human's single command; clean build/up, wait,
  black-box assertions, evidence export, and unconditional teardown.
- `docs/meteorite.md`: prerequisites, exact command, optional live Telegram
  probe, expected evidence, recovery/rollback and secret injection rules.
- `vendor/old-contracts/PARITY.md`: every donor contract classified
  `implemented-tonight`, `D1–D7`, or `rejected-with-reason`; no silent omission.

### Decidable meteorite gate

Human command from a clean clone of `v3`:

```sh
git switch v3
test -z "$(git status --porcelain)"
./scripts/meteorite.sh
```

`scripts/meteorite.sh` must exit non-zero on any missing assertion and retain an
evidence bundle under a temporary/export directory named by source SHA. It must:

1. build with network only for declared dependency/image resolution, then run
   the test with no repository-external configuration;
2. run container installation twice and prove identical manifest/state;
3. start fake Telegram and control-plane health, inject one Human message, prove
   durable inbox receipt precedes acknowledgement, and receive truthful `/status`;
4. enqueue one mission, prove `orchestrator→manager→worker` parentage, worker
   acknowledgement, artifact plus terminal report, and semantic generation
   advancement;
5. kill the dispatcher after worker start, restart it, prove no duplicate worker
   and successful terminal reconciliation;
6. force Telegram send failure, prove persisted retry/degraded status and an
   eventual exactly-once delivery after recovery;
7. inject stale semantic progress while processes remain alive and prove the
   supervisor reports/escalates `NO-GO`, not green;
8. assert zero secrets/image credentials, declared ports only, resource limits,
   non-root processes, clean teardown, and no surviving containers/volumes;
9. print source SHA, image digest, each assertion, terminal report SHA/evidence,
   teardown result and final `METEORITE: PASS`.

The optional live probe is
`TELEGRAM_BOT_TOKEN=... TELEGRAM_CHAT_ID=... ./scripts/meteorite.sh --live-telegram`;
it never prints/persists those values and is not required for deterministic
green. Tonight's result is `NO-GO` for cutover even when this slice passes.

## Tonight's lane wave (`--base v3`)

Dispatch all L0 lanes together. Dependencies are artifact/contract dependencies,
not permission to share paths. Each lane owns only its listed paths and commits
`[CODER]`; Tier-A integration receives independent exact-SHA review before land.

| Lane | Role | Exclusive scope | Depends | Done criterion | Budget |
|---|---|---|---|---|---|
| `v3-foundation` | coder | root toolchain files; `instructions/**`; `instance/**`; `tools/instructions/**`; `vendor/old-contracts/**` | none | NEW allowlist and donor fixtures copied, SHA provenance + parity classifications present, composer/check tests green | 45–60m |
| `v3-state` | coder | `core/**` | none | fresh hierarchy/evidence/outbox schema supports fenced transition and restart reconstruction; red/green unit tests | 45–60m |
| `v3-container` | coder | `docker/**`; `bootstrap/container-install.ts` | none | image builds non-root; install twice is identical; manifest proves tracked config only | 45–60m |
| `v3-telegram` | coder | `daemon/**` | none | fake endpoint accepts inbound, durable-before-ack holds, failed outbound persists/retries exactly once, truthful degraded status | 45–60m |
| `v3-dispatch` | coder | `orchestrator/dispatcher*`; `tests/fixtures/noop-worker.ts` | state contract supplied in mission | confirmed launch + terminal evidence; dispatcher-death test produces no duplicate | 45–60m |
| `v3-recovery` | coder | all other `orchestrator/**` including supervisor/status/recovery copied slice | state contract supplied in mission | stale semantic progress is red despite live PID; restart fences old owner; independent escalation recorded | 45–60m |
| `v3-meteorite` | integration-test coder | `tests/meteorite.test.ts`; `scripts/meteorite.sh`; `docs/meteorite.md` | published contracts from all L0 lanes; starts immediately with fakes | black-box script encodes all nine assertions and always tears down | 45–60m |
| `v3-review` | independent Tier-A reviewer | `orchestrator/runtime/reviews/**` only | candidate SHAs for preceding lanes | exact-SHA security, runtime/operations and false-green review; `ACCEPT`/`REJECT` with rerunnable commands | 30–60m |

Landing order: foundation → state → container/telegram (either order after
foundation) → dispatch/recovery (either order after state) → meteorite → review
fixes → one serialized `gate/land-batch.sh` or individual `gate/land.sh` queue →
canonical clean-clone meteorite rerun. Review work does not modify implementation
paths. If fewer than three lanes remain while ready rows exist, dispatcher state
must show a fault; if no ready rows exist, Telegram must request more work.

## D1–D7 runway after tonight

### D1 — inventory, red locks, schema freeze

Complete two-tree/rescue capability inventory. Every OLD capability gets donor
SHA/path, NEW counterpart, disposition, owner, reviewer, rollback and executable
red-before lock. Freeze mission→manager→lane schema, global 10–12 capacity budget,
per-manager cap, maximum depth 3, explicit spawn-rights matrix, path capabilities,
semantic generation and recursive cleanup. Gate: zero unknown/unclassified rows.

### D2 — recovery and channel spine

Split transport/command/mission/outbox boundaries out of the daemon monolith;
land/deploy safe failure domains, durable inbound/outbox, direct independent
Telegram escalation, provider lock identity, bounded restart and installed-unit
parity. Rehearse daemon/provider/dispatcher death and inherited-lock regression in
daylight. Gate: channel restart cannot kill scheduler/managers/workers; alerts do
not traverse the watched pane.

### D3 — continuous queue and three-level hierarchy

Port OLD queue/manager behavior behind NEW state/contracts. Scheduler maintains
10–12 useful leased slots, normally 2–3 managers with 3–4 workers each; only
declared manager/research/review roles may spawn level 3; children count against
the global budget. Confirm first-token ack, next wave, below-three alert,
end-of-queue request, terminal SHA/zero-commit detection and recursive reaping.

### D4 — parallel evidence, serialized landing

Run candidate verification concurrently in clean isolated clones and cache only
by tree+command digest. Serialize exact-SHA review confirmation, merge,
post-merge verification, deploy and cleanup. Prove stale evidence cannot survive
rebase/change, and branch/worktree reaping retains all unmerged or failed refs.

### D5 — daylight chaos and meteorite rehearsal

From a clean clone, kill worker, manager, top session and channel independently;
force launch failure, expired lease, zero-commit exit, empty queue, malformed
pack, stalled-live process, daemon restart, leaked-lock predecessor and rollback.
Verify bounded recovery or loud independent escalation, no healthy-work kill, no
leaked unit/worktree/branch, and host/tracked-unit parity.

### D6 — first trustworthy overnight run (gated milestone)

Admission requires D1–D5 at one independently reviewed SHA, clean-clone install,
all chaos drills green, tracked/installed parity, finite timer triggers, and 6–8
bounded independent missions plus ready reviews. During the run the supervisor
requires newer durable semantic generations by deadlines, refills useful slots
to 10–12, and pages independently on bounded retry exhaustion or empty queue.
Morning evidence maps every admitted row one-to-one to terminal SHA, review,
result, retries, alert delivery and cleanup. Any missing row or unexplained gap is
`NO-GO`; heartbeat/timer/Telegram delivery alone is never progress.

### D7 — cutover decision

Independent security, operations/runtime and regression reviews inspect the same
SHA and overnight bundle. Repeat clean-clone bootstrap, rollback and teardown.
Only then decide whether `v3` becomes main/canonical; that branch/cutover decision
remains Human-controlled. Otherwise retain evidence, classify the exact failed
gate and continue bounded fixes without declaring success.

## Hygiene v3 must not inherit

| Failure | v3 prevention rule | Decidable evidence |
|---|---|---|
| Branch breeding (~250 stale `ag-*` refs) | Each lane branch has mission/owner/lease/terminal disposition. Landing gate serializes merge and reaps only accepted landed refs; failed/unmerged refs retain an explicit reason and expiry. A daily inventory is fail-closed on unexplained refs. | Clean-clone test creates landed, failed and protected refs; only landed ref/worktree is removed; inventory count reconciles exactly. No OLD refs/history are imported. |
| Silent stalls | Progress is a durable generation backed by artifact/terminal SHA or declared healthy long check. Independent supervisor requires ack/new generation by deadline, retries boundedly, then pages outside the watched pane until recovery; empty queue asks the Human for work. | Live-but-stale worker and killed dispatcher drills produce `NO-GO`/page; restart reconstructs once without duplication; overnight ledger has no unaccounted interval. |
| False green | Process, timer, channel delivery, worker ack and semantic progress are separate fields. Unknown/missing/stale/timed-out evidence is `NO-GO`; only exact-SHA canonical reruns and Tier-A review permit landing/cutover. | Red-before tests for tmux-send success, missing timer, delivery-only status, zero-commit and partial Docker output; each must fail before and pass after its slice. |
| Host-only mechanisms | Every script, image, compose file, unit template, config schema, install/recovery/rollback step and decision rule is tracked. Secrets and host values are enumerated/injected, never baked in. Runtime drift is a blocking defect. | Fresh clone with only Docker+Bun/Git prerequisites runs `./scripts/meteorite.sh`; second install is idempotent; image digest/evidence recorded; teardown leaves no undeclared state. |

## Terminal disposition

The plan artifact is complete and clean as synthesis. Infrastructure v3 remains
`NO-GO` for cutover/unattended work until the gates above pass; tonight's bounded
next action is the eight-lane wave and Docker meteorite proof.

commit: e695702faf5adbb53a8e0e9698b44e7c4643711d v3 plan synthesis
verify: test "$(git rev-parse HEAD)" = e695702faf5adbb53a8e0e9698b44e7c4643711d && test "$(git -C /root/legacy-donors/bpa-master rev-parse HEAD)" = d0a99b8439f2731654e23b5e7759961f4602d0d3 && test -s orchestrator/runtime/reports/v3-plan-synthesis.report.md && rg -n '^(## Consumption check|## Tonight.s exact slice|## Tonight.s lane wave|## D1.D7 runway|## Hygiene v3 must not inherit|commit:|verify:|result:|secret-scan:|remaining:)' orchestrator/runtime/reports/v3-plan-synthesis.report.md
result: clean
secret-scan: clean
remaining: none
