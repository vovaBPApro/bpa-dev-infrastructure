# Branch names hygiene/reap.sh refuses to delete unconditionally, regardless
# of merge status. Read by hygiene/reap.sh's `load_protected` (one name per
# line, `#` comments and blank lines ignored). The mechanism (hygiene/reap.sh)
# is generic; this file is the this-installation fact it needs, per
# CLAUDE.md Mission ("that is what instance/ exists to absorb").
#
# The default branch itself (--main, default "main") is always protected in
# code and does not need an entry here.

# The v2 line: read-only donor, retained for parity checks and the one copy
# of pre-v3 mechanisms not yet ported (instance/workboard.md Phase 1).
v2-deprecated

# The v3 orphan-history root. Not otherwise reachable from `main` by
# merge-base, so it would never be caught by the ancestor check anyway, but
# an explicit refusal here means that stays true even if that ever changes.
v3

# Parked work retained under HR-1726's three-round exit. Unmerged by design:
# the branch IS the evidence and the starting point for the recut, so an
# ancestor check would happily delete it. Remove this entry only when the row
# is recut and landed, or when the park record is deliberately abandoned.
# Park record: instance/parked/V3-1.1-stage2-2026-08-03.md
ag-s3-2-r3

# V3-0.23 round 3 — shell-tier concurrency. PARKED at the HR-1726 cap: the lock
# validates a proxy (a variable) rather than the ref actually created, defeated in
# three successive spellings. Park record:
# instance/parked/V3-0.23-shell-tier-concurrency-2026-08-04.md
ag-s7-7

# V3-1.9b round 3 — the lane network boundary. PARKED at the HR-1726 cap: the
# root-manager 0/28 baseline stopped reproducing, so the only known-good signal is
# unstable. Park record:
# instance/parked/V3-1.9b-network-boundary-2026-08-03.md
ag-s6-21

# V3-2.3 round 3 — the deployment drift guard. PARKED at the HR-1726 cap: the
# arming lock mocks systemd instead of executing it, so the circularity moved
# rather than closing. Park record:
# instance/parked/V3-2.3-drift-guard-2026-08-03.md
ag-s6-12

# V3-3.4's parked branch ag-s6-13 was unprotected on 2026-08-04: the row landed at
# 7c428e0, so the branch no longer holds the only copy of anything.

# V3-1.9 round 3 — the branch that carries the correct single-service-user model.
# PARKED at the HR-1726 cap: the remaining blocker is operator provisioning of the
# service account and its credentials, not more code. This branch IS the evidence
# and the starting point for the recut. Park record:
# instance/parked/V3-1.9-nonroot-lanes-2026-08-03.md
ag-s5-1

# Round 2 of V3-1.9 (non-root lanes), reported NO-GO and not landed. Retained
# because round 3 rebuilds on a different model (the single-service-user shape
# in instance/v3-nonroot-lane-model-2026-08-03.md) and this branch is the only
# copy of what round 2 established under the old one. Remove this entry when
# V3-1.9 lands or parks with its own record.
ag-s4-5-r2

# ag-s3-1-r3 was retained here while V3-1.5 was parked. The recut landed at
# 133541c and meteorite/run.sh + prove-candidate.sh are on main, so the branch
# no longer holds the only copy of anything. Unprotected 2026-08-03 at the
# operator's question ("ці для чого захищати??", Telegram 1931). It remains on
# origin as history; nothing depends on it.
