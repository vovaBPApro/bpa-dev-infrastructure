# V3-3.5 — explained decision options: PARKED at the cap, 2026-08-04

Three rounds under HR-1726, no ACCEPT. Retained branch `ag-s5-10-r3`, tip
`027096685b76772c931c4603f56961b3900c269e`, pushed to origin and protected. **Do not
reap.** Round-3 review: `/root/.cache/infra-lanes/ag-s5-10-r3.review.md`.

## The row

Operator ruling **HR-1752**. A Telegram button label is about three legible words, so the
Human chooses between options he cannot fully read, with the reasoning nowhere — and he
is asked only for the **irreversible** set. This path carried both of today's operator
questions: the `bpa` provisioning decision (HR-1962) and the still-open V3-1.9 park.

## What is genuinely built and verified — this is most of the row

The round-3 reviewer confirmed by execution, and this should not be re-derived:

- **The lock traverses production registration, not a test re-registration.**
  `daemon/server.ts:1182-1223` is the real `request_decision` case; `:1310-1326`
  constructs the normal MCP server and fetches its registered `tools/call` handler;
  `:1338-1355` feeds an update through grammY; `:3304-3338` is the registered callback
  listener. The reviewed lock exercised both seams and observed the real
  `bot.api.sendMessage` payload, the real `InlineKeyboard`, the callback answer, the
  buffer, a stale sid and an unknown sid.
- **Order equivalence holds beyond the weak two-option case.** The reviewer wrote its own
  probe — four reordered options with deliberately similar three-word labels
  (`Alpha shared tail`, `Alpha shared tale`, `Beta shared tail`, `Alpha shard tail`) —
  and asserted exact body order, exact flattened keyboard order, callback indices
  `0,1,2,3`, then selected index 2 through the registered grammY callback and observed
  `decision:probe_order=v4`.
- Earlier rounds closed: duplicate wire values refused; empty, whitespace-only and
  malformed decision ids refused at **both** composition and resolution; composition
  limits refusing without truncating meaning; malformed input producing **zero**
  `sendMessage` calls.

## The open blocker

**Round 3's own delta has no behavioural red-before.** `daemon/server-decision.test.ts:1-34`
only moves the server import and test-seam installation from `beforeAll` to module scope.
On its actual parent `a12dd4b3ec282d592efef567a357c98b4f81381e` the unchanged lock
already passed standalone (2 pass, 0 fail) and the full suite passed (548 pass, 3 skip,
0 fail). So the round-3 change cannot satisfy the mandatory fail-before requirement.

## An orchestrator error contributed, and is recorded here

The round-3 brief named `a5caa5698099ed90ca4e3f1bf2173c90b9211424` as the parent to prove
red-before against. That SHA is **not an ancestor** of this branch (`merge-base
--is-ancestor` exit 1; merge base `88f696d4`), does not contain
`daemon/server-decision.test.ts`, and lacks the exported registered-handler seams —
applying the final test there yields the missing-export/import failure the brief itself
prohibited. The chain had moved on and the orchestrator did not check before naming the
SHA.

That does not make the finding wrong: a round with no behavioural red-before is
unlandable regardless of which parent is named. But a recut should not spend a round
re-discovering the ancestry.

## The lock the recut must satisfy

Identify what behaviour, if any, round 3 actually changes, and produce a red-before for
**that** — against its true parent. If the answer is that the wiring was already complete
at `a12dd4b3` and round 3 is purely test-initialisation hygiene, then the landable unit
is the earlier work plus a regression proving the wiring, and the initialisation change
should be justified on its own terms (does the module-scope seam alter behaviour for
other suites?) rather than carried as if it were the fix.

Related: HR-1752 (the ruling), V3-0.22 (moved-not-closed, landed `6257a32`), and the
live use of this path in `instance/decisions/HR-1962.md`.
