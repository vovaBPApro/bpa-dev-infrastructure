---
id: hr-11552
layer: L1
status: binding
audience: all
tags: [instance, decision]
verbatim: not-captured
date: 2026-07-29
routes-to: branching-policy
state: routed
---

# HR-11552 — Staging stand, batch landing, one canonical main

verbatim: not-captured. No verbatim quote for msg 11552 exists in this repo's
docs; `instructions/repo-layout.md` and `instructions/branching-policy.md` cite
the 11552–11555 range but quote only msg 11555 (see HR-11555). The line below is
a PARAPHRASE, not the Human's words.

**Paraphrase (labelled — NOT verbatim):** decided as part of the 11552–11555
range on branching and layout: a staging stand runs current `main`, reviewed
disjoint lanes may land as a serialized batch, and there is a single canonical
integration line (`main`) rather than long-lived `dev`/`staging` branches.

routes-to: `branching-policy` (`instructions/branching-policy.md` — one
long-lived `main`, batch landing of up to 3 disjoint lanes, staging as a runtime
target on `main`).
