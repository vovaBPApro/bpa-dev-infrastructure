# HR-2171 — two startup states, and off-host backup of the state

Recorded 2026-08-04 from Telegram message 2171, in reply to the orchestrator's
measurement of what lives outside git (message 2167).

## The operator's words, verbatim

> Весь код и правило, но нет нашей ответы, что было в работе и чему. Ну в
> принципе это не проблема, я его так и задумывал, мне как бы, ну это же больше
> было друзьям скинуть, то есть им как раз и нужно, чтобы у проекта по идее был
> стейт, когда он свежий. Выглядит так, что нам в принципе два стейта у проекта
> нужно. Когда он свежий нулевый и тогда он начинает с З того, що опитує
> користувача, тіпа, щоб, да, там, зрозуміти, на якій машині воно запускається,
> погодити ключові якісь підходи, да, запропонувати йому стратегію під його
> залізо, як арх-- як інфраструктура буде працювати, за якими правилами і з якою
> візією. Він може, если що, його відредагувати і затвердити. Ну от редагувати
> необов'язково. Затвердити, ну да, треба затвердити обов'язково виходить От, ну
> і тіпа, да, з нуля все піднімається. Если що, якийсь онбординг повноцінний ми ж
> там прописували. А виходить другий стейт. Це да, це дуже корисно, коли є
> вихідні дані, з чого йому ініціалізуватись. Слухай, ну давай на Google Диск
> його писати. В тебе ж є доступ до Google Диска? Пиши на Google Диск. Просто в
> My Drive папочку створюй і пиши туди. Або пиши як бекапи, просто бекап. Навіть
> краще, да, пиши локально, як ти звик працювать, а туди просто бекап раз на
> годину нехай. Я ж так розумію, там не дуже великі файли. Правильно? Їх можна
> часто бекапіть без проблем. Можна навіть зберігати, не знаю, десять останніх
> версій чисто потому що можем собі позволить і це реально нескладно там буде Я
> думаю, це нескладно реалізувати. Ну, взагалі так, дуже класно це зберігати, бо
> дійсно може бути таке, що сервер там упав, пропав, і я тоді, ну, можу підняти
> його, дати доступ до того самого Google Drive і оці бекапи через нього ж і
> відновити. Получається, да, він при запуску просто питає ти типу як: "Хоч єсть
> у тебе бекап-файли чи ні? Если єсть, то тоді от как би можна їх йому
> згодувати". Да, по дефолту він каже, що він зберігає на Google Диску, тіпа
> можеш дати доступ до Google Диска, допомагає провести по цьому процесу. І тоді
> він уже йде на Google Диск, і там знаходяться файли. Він знатиме й найменування,
> і в якій папці шукати, бо по факту ж це, ну, прописані правила, як це
> записувати. Якщо це зафіксовано, то він і сам легко розбереться, як його
> прочитати

## What it requires

**Two startup states, both first-class.**

1. **Fresh, zero state.** The installation interrogates the operator: what machine
   is this, what are the key approaches, and it *proposes a strategy fitted to the
   hardware* — how the infrastructure will work, under what rules, with what
   vision. The operator may edit it; **approval is mandatory, editing is not**.
   Then it comes up from nothing. This is the onboarding already specified, and it
   is the same requirement as HR-2120 (the installation must measure the machine
   it lands on and negotiate limits with the installer).
2. **Restore from prior data.** At startup the installation asks whether backup
   files exist. By default it states that it backs up to Google Drive, offers to
   walk the operator through granting access, then locates the files itself —
   because the naming and folder convention is a written rule, so a reader that
   has the rules can find and read them without being told.

**Backup.** Keep writing locally as now; back up to Google Drive **hourly**, into
a folder the installation creates under My Drive. Retain roughly ten versions —
the operator's reasoning is that the files are small enough that frequency and
retention are free.

**Why it matters, in his words**: the server may fall over or disappear; he brings
up a new one, grants it access to the same Drive, and restores through it.

## Measured, so the design rests on numbers

- `/root/.local/state/bpa` — **110 592 bytes** (`state.db`, `state.db-wal`,
  `state.db-shm`).
- `/root/.local/state/bpa-dev-infrastructure` — **1 064 bytes**
  (`evidence/meteorite-latest.md`).

So the operator's assumption is correct: hourly backup with ten retained versions
is roughly a megabyte of Drive per two days. Frequency is not the constraint.

- Secret scan over `state.db` and its WAL using the canonical pattern from
  `gate/land-lib.sh: land_secret_scan()`, each component checked individually:
  **no hits**. This must be re-verified by the implementing lane rather than
  trusted, and it must be re-verified *continuously* — state grows, and the day it
  contains a token is the day an hourly job publishes it.

## The boundary the orchestrator must not blur

The orchestrator's own session has Google Drive tools. **That is not the machine
having access.** A backup that runs through an interactively-authenticated session
dies with the session, is absent in headless and cron runs, and cannot be
reconstructed from the repository — which makes it a host-only mechanism and a
Hard Floor 5 defect, exactly the class this row exists to close.

For the installation to back itself up, it needs **its own credential**, provisioned
by the operator. That is V3-2.10, and secret provisioning is one of the few things
the orchestrator is required to ask for (Hard Rule 14).

Two further constraints on the implementation:

- **Backing up is publishing.** Anything written to Drive leaves this host and may
  be retained or indexed after deletion. The enumeration in V3-2.9 must therefore
  say, per item, whether it is safe to send off-host — not only whether it must
  survive.
- **`state.db` alone is not a backup.** Writes are in flight through a WAL; a
  consistent copy needs sqlite's own backup path or a checkpoint.

## Amendment, Telegram 2178 and 2179 — cadence, and what the access actually is

### The operator's words, verbatim

> ну тоді бекапи можемо робити кожні п'ять хвилин.я думаю, для google це невелике
> навантаження по запитах

> мне издавалось, ты вжимаешь доступ к google диску через сервис-аккаунт. так

### Cadence: every five minutes, not hourly

288 uploads a day of a file this size is not a load worth designing around, and
the operator is right that it is free.

But retention must change with it: **ten versions at a five-minute cadence is
fifty minutes of history**, which would lose a whole working session. The
orchestrator proposed, and the operator has not yet ruled on, a two-tier
retention: the last ten snapshots plus hourly snapshots across a day. Whoever
implements this must state the retention it chose and the window it actually
covers, in time rather than in file count — a count is not a horizon.

### The access correction

The operator believed the orchestrator reaches Drive through a service account.
**It does not.** The access is the Google Drive integration attached to his
claude.ai account — an interactive OAuth connection belonging to this session.
There is no service account on this host and no Drive credential in the
installation.

Consequences for the design, which the implementing lane must respect:

- A service account's files land in **the service account's own Drive**, not in
  the operator's. The folder must be created in his My Drive and **shared with the
  service account's address**, or he will never see the backups he is relying on.
- Service accounts have their own storage quota and are not covered by a personal
  Drive plan. At roughly a megabyte a day this does not bind, but the restore path
  must not assume otherwise.
- The credential is the operator's to create (Hard Rule 14, secret provisioning),
  and it belongs in V3-2.10's runbook: what it is, where it goes, what scope it
  needs, and the command that verifies it. Drive scope should be the narrowest
  that permits writing to one folder — not full-account access.

## Correction, Telegram 2190 — the service account exists, and the orchestrator was wrong

### The operator's words, verbatim

> та ні, гані, я ж тебе налаштовував, давав тобі ключик оцей json сервісного
> аккаунта.
>
> якщо його в тебе нема, то візьми у старого оркестратора, у нього точно є, бо це,
> можливо, він тобі його налаштовував. але він точно був у тебе. це я на тисячу
> відсотків впевнений.
>
> просто я знаю, що якщо що, він і у того має бути, тож можеш у нього попросити.
>
> та да, да, створив, розшарив сервісний аккаунт, всьо класно. все здорово.
>
> папку, ну, папку, якщо що, розшарю, але ну хоча б давай дотягнись, попробуй
> щось там зробити, бо ти сервісний акаунт ще не бачиш, а він є.
>
> нє, нє, нє, останні десять версій ти зберігаєш на цьому, на google диску. ти їх
> просто перетираєш, переписуєш.
>
> одну додаєш, одну видаляєш, коли накопичились, і всьо. ти не так поняв про
> десять версій. нащо ти ускладняєш?

### The orchestrator was wrong

It told the operator twice that there was no service account on this host. There
is. It was found by searching, exactly where he said it would be:

```
/root/.config/bpa/oauth/gcp-sa-bpapro-agents.json
  type         : service_account
  project_id   : bpapro-agents
  client_email : bpa-dev-orch@bpapro-agents.iam.gserviceaccount.com
```

The identifiers above are not secrets and are recorded here because the folder
must be shared to that address for the backup to be visible to the operator. **The
private key was not read out, is not recorded anywhere, and must never enter git.**

The correct statement, which the wrong one was reaching for, is narrower and still
holds: the *session's* Google Drive tools are an interactive OAuth connection and
are not available to a headless or cron run. The machine's own path to Drive is
this key — which makes it non-git host state that V3-2.9 must enumerate and V3-2.10
must own.

One consequence to design against: **the key must not be backed up to the Drive it
authenticates to.** A restore that needs the key to fetch the key is not a restore.
It belongs in the credential runbook and in the operator's hands, not in the
snapshot.

### Retention: simple rotation, not tiers

The orchestrator proposed a two-tier retention (last ten plus hourly across a day).
The operator rejected it as needless complexity: keep the last ten on Drive, add
one, delete the oldest, done.

He is right that the simpler rule is the one to build, and the earlier note about
a fifty-minute horizon is a **fact to state, not a reason to complicate the
design** — at a five-minute cadence, ten slots is fifty minutes, and the operator
has chosen that knowingly. Anyone changing the cadence later should re-derive the
horizon rather than assume it.

## Related

- HR-2120 — the installation measures its machine and negotiates limits with the
  installer. State 1 above is that requirement seen from the operator's side.
- V3-2.9 — enumerate non-git host state with a verification command each. This
  ruling makes off-host safety a required column.
- V3-2.10 — credential provisioning and rotation. The Drive credential belongs
  here, and it is the operator's to create.
- HR-1720 — host deployment deferred; prove in a container or fixture first.

## Simplification, Telegram 2268 — the operator is right and the orchestrator over-built it

### His words, verbatim

> бекап і відновлення - це можемо реалізувати після переходу на новий сервер, але я
> все ж не розумію чого там так складно, ти вже в якомусь вигляді всі ці файли
> зберігаєш, що як нам просто архівувати папку і бекапити архів в гугл драйв і все,
> і тоді відновлення не дуже і треба бо ти стартуєш вже з памяттю... хоча так,
> відновлення може і треба щоб можна було розшировати на інщщі варіанти рестора...
> але ж це не має бути настільки складно

### What changes

**Archive the directory, upload the archive, done.** Restore is then unpacking, not a
subsystem — the installation starts with its memory already in place. He allows that a
restore path is still worth having later, to support other restore targets, but it is not
what makes the first version work.

The orchestrator wrote this row around WAL consistency, sqlite backup APIs and two-tier
retention. For **110 592 bytes** of state that is over-built: stop the writer for a
moment, pack the directory, upload. The consistency question the earlier text agonised
over disappears when nothing is writing during the copy.

**Sequencing also changes**: this may land *after* the move to the new server. It is not a
cutover prerequisite in its simple form, because a fresh install that starts with an
unpacked archive has its memory regardless.

### What survives from the earlier text, and only this

- The machine needs its **own** Drive credential; the session's tools are not the machine's
  (V3-2.10). That is unchanged and is still the operator's to provision.
- **The key must not be backed up to the Drive it authenticates to** — a restore that needs
  the key to fetch the key is not a restore.
- **Backing up is publishing**: whatever goes into the archive leaves this host, so the
  V3-2.9 enumeration must still say, per item, whether it is safe to send off-host. The
  secret-bearing `orchestrator.env.bak-20260730` found on this machine is exactly why.
- Retention stays the simple rotation of ten he already ruled (Telegram 2190).
