# HR-2315 — Pin Opus as the resting top-orchestrator model

date: 2026-08-04
source: Telegram, message 2315
status: binding
supersedes: HR-709 item 1 (resting model Fable) — resting-tier choice only. HR-709 items 2–4 (Codex gpt-5.6-sol is quota-exhaustion fallback only; children on Codex; children lean/autonomous) are untouched and stand.

## Human words (verbatim, do not reword)

> Та бляха-муха, що ти все на Fable піднімаєшся?Запінь уже опус

Context — his immediately preceding message asked which model the session was
running on; the orchestrator answered `claude-fable-5` (the HR-709 resting pin).
This reply orders the pin moved to Opus.

## Ruling

1. **Resting top-orchestrator model is `claude-opus-5`.** Supersedes the
   HR-709 resting choice (`claude-fable-5`). Do not restore Fable (or any other
   value) without a newer HR row.
2. Everything else in HR-709 stands: Codex `gpt-5.6-sol` remains a
   quota-exhaustion fallback only, child/lane agents stay on Codex and stay
   lean so they do not round-trip the top orchestrator.

## Applied

`orchestrator/runtime.env` (gitignored host state; template
`runtime.env.example`) updated in the same turn:
`ORCH_CLAUDE_MODEL=claude-opus-5`. Per the model-registry design the switch is
relaunch-scoped — the session live at the time of this ruling continues on
`claude-fable-5` until the next orchestrator start (`/restart`).

Related: [[HR-709]], [[HR-269]], [[HR-271]], `daemon/model-registry.ts`.
