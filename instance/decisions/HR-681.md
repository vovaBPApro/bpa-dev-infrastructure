# HR-681 — In the old repos, read the `dev` branches. Only dev.

date: 2026-08-01
source: Telegram, message 681
status: binding

## Human words (verbatim, do not reword)

> Запиши собі десь в інструкціі - в старих репо дивись бранчі дев! Тільки дев!!!!

Context — his immediately preceding instruction (HR-535) was:

> Передивитись плани в старих репо, і реалізовані і відкриті

## Ruling

When mining the legacy donor repositories for plans, requirements, complaints or
prior implementations, read `origin/dev`. Not `main`.

## Why this matters (measured 2026-08-01)

`main` in the donors is STALE, and the gap is not marginal:

| donor | main tip | dev tip | dev ahead of main |
|---|---|---|---|
| bpa-master / bpa-shell | d0a99b8 (2026-07-24) | 877ce807 (2026-07-29) | **134 commits** |
| agent-bill | b2b00eb (2026-07-23) | 92f7ec479 (2026-07-29) | 1 commit |
| agent-mila | 7d1a173 (2026-07-23) | edc5a4c (2026-07-29) | 2 commits |

The tip of `dev` in all three is `[ORCH] backlog: file product complaints from
2026-07-29 history` — his filed product complaints, which HR-535 explicitly asks
for ("знайти на що я скаржився стосовно форми новоі транзакціі"). Reading `main`
missed them entirely.

## Standing rule

- donor discovery reads `origin/dev` first; `main` is a fallback, not the source
- never push, never modify, never check out over the donors' current HEAD —
  read with `git -C <repo> show <ref>:<path>` and `git log`
- donors: `/root/legacy-donors/{bpa-master,agent-bill,agent-mila}`.
  `/srv/archive/*` are duplicates of the same three — do not double-count.
