# Runtime capture artifacts — raw inbound chat, never committed (secret-hygiene:
# the jsonl carries verbatim Human messages that may contain sensitive words).
# HR-*.md decision files ARE tracked; the .jsonl append logs are not.
inbox.jsonl
triage.jsonl
