{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bpa.dev/schemas/orchestrator-handoff-v1.json",
  "title": "Orchestrator vendor/session handoff",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "source_sha",
    "timestamp",
    "from",
    "to",
    "from_vendor",
    "from_session",
    "to_vendor",
    "to_session",
    "worktrees",
    "unlanded_reports",
    "open_decisions"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "source_sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "timestamp": { "type": "string", "format": "date-time" },
    "from": { "type": "string", "minLength": 1 },
    "to": { "type": "string", "minLength": 1 },
    "from_vendor": { "type": "string", "minLength": 1 },
    "from_session": { "type": "string", "minLength": 1 },
    "to_vendor": { "type": "string", "minLength": 1 },
    "to_session": { "type": "string", "minLength": 1 },
    "worktrees": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "head", "branch"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "head": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
          "branch": { "type": ["string", "null"] }
        }
      }
    },
    "unlanded_reports": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "open_decisions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "path"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "path": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
