# One argv entry per line. This installation uses Codex for worker lanes; the
# generic launcher treats this file as data and does not know the provider.
#
# 2026-08-04, V3-0.51 — foreground-command bound: MEASURED, not assumed. The
# claude-family confs must declare BASH_DEFAULT_TIMEOUT_MS/BASH_MAX_TIMEOUT_MS
# because that harness kills a foreground command at 120000 ms undeclared. This
# CLI does not: probed with a 150 s foreground command (via the spark variant,
# same CLI, separate quota bucket) it completed, so no bound entry is set here.
# Re-measure with
#   orchestrator/fleet/probe-foreground-bound.sh --agent-command <this file> \
#     --sleep-seconds 150 --expect completed
# Note what is NOT claimed: that this CLI has no bound at all, only that it did
# not kill at 150 s.
codex
exec
--dangerously-bypass-approvals-and-sandbox
