# Governance (archived stub)

Former draft governance notes. Binding law now lives in `instructions/`: roles
and dispatch/review boundaries in `roles`, branch model in `branching-policy`,
correlation-id and fenced state transitions in `lane-lifecycle`, secrets/live-data
posture in `repository-hygiene` and `repo-layout`. The clean-host bootstrap
design is `migration-prep/UNIVERSAL_PROJECT_PLAN.md`.
